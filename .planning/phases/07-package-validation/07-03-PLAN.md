---
phase: 07-package-validation
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified: []
autonomous: true
requirements:
  - PKGP-02
  - PKGP-05
must_haves:
  truths:
    - "publint reports zero errors and zero warnings"
    - "attw reports no type resolution problems for node16 and bundler modes"
    - "Zero any types exist in source code (AbortSignal.any() is a method name, not a type)"
    - "npm pack tarball contains exactly the expected files"
    - "prepublishOnly pipeline runs successfully end-to-end"
  artifacts: []
  key_links:
    - from: "package.json prepublishOnly"
      to: "publint validation"
      via: "npx publint at end of pipeline"
      pattern: "npx publint"
---

<objective>
Run all package validators end-to-end and confirm the package is structurally correct for every consumer configuration. This is the final validation gate — no code changes expected, only verification.

Purpose: Prove all PKGP requirements are met by running publint, attw, any-audit, tarball inspection, and the full prepublishOnly pipeline.
Output: Verification results confirming package readiness
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-package-validation/07-CONTEXT.md
@.planning/phases/07-package-validation/07-01-SUMMARY.md
@.planning/phases/07-package-validation/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run publint, attw, and any-audit validators</name>
  <files></files>
  <read_first>package.json, tsconfig.json</read_first>
  <action>
    Run the following validators in sequence and confirm all pass:

    1. **Build first** (validators need dist/): `npm run build`

    2. **publint** (PKGP-02): `npx publint`
       Expected: "All good" with zero errors and zero warnings.
       If any issues found, fix them in package.json or tsup.config.ts before proceeding.

    3. **attw** (PKGP-02): `npx @arethetypeswrong/cli --pack .`
       Expected: "No problems found" for node10, node16-CJS, node16-ESM, and bundler resolution modes.
       If any issues found, fix the exports map or type declarations.

    4. **Zero any audit** (PKGP-05): `npx biome check src/`
       Expected: No `noExplicitAny` violations. The only `any` in source is `AbortSignal.any()` which is a method name per D-06, not a type annotation.
       Additionally run: `grep -rn ": any" src/ --include="*.ts"` and `grep -rn "as any" src/ --include="*.ts"` to catch anything biome might miss.
       Expected: zero matches (AbortSignal.any is `.any(` not `: any` or `as any`).

    5. **Tarball inspection** (PKGP-04): `npm pack --dry-run 2>&1`
       Expected files in tarball:
       - `dist/index.js` (ESM)
       - `dist/index.cjs` (CJS)
       - `dist/index.d.ts` (ESM types)
       - `dist/index.d.cts` (CJS types)
       - `dist/index.js.map` (ESM sourcemap)
       - `dist/index.cjs.map` (CJS sourcemap)
       - `README.md`
       - `CHANGELOG.md`
       - `LICENSE`
       - `package.json`
       Must NOT contain: README.en.md, src/, tests/, examples/, docs/, .planning/, .env*, biome.json, tsconfig.json, tsup.config.ts, vitest.config.ts, typedoc.json, coverage/

    6. **prepublishOnly pipeline** (PKGP-03): `npm run prepublishOnly`
       Expected: All 5 stages pass (lint -> typecheck -> test -> build -> publint).
       This is the ultimate gate — if this passes, the package is publish-ready.

    If any validator fails, diagnose and fix the issue. The fix should be minimal and targeted.
  </action>
  <verify>
    <automated>npm run build && npx publint && npx @arethetypeswrong/cli --pack . 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx publint` exits 0 and output contains no "error" or "warning" lines
    - `npx @arethetypeswrong/cli --pack .` exits 0 and output contains "No problems found" or shows all green checkmarks
    - `grep -rn ": any" src/ --include="*.ts"` returns zero matches
    - `grep -rn "as any" src/ --include="*.ts"` returns zero matches
    - `npm pack --dry-run 2>&1` does NOT contain "README.en.md" or "src/" or "tests/" or ".planning/" or "examples/"
    - `npm pack --dry-run 2>&1` DOES contain "CHANGELOG.md"
    - `npm run prepublishOnly` exits 0
  </acceptance_criteria>
  <done>All package validators pass: publint clean, attw clean, zero any types, tarball clean, prepublishOnly pipeline green</done>
</task>

</tasks>

<verification>
- `npm run prepublishOnly` exits 0 (runs full pipeline: lint + typecheck + test + build + publint)
- `npx @arethetypeswrong/cli --pack .` exits 0
- `npm pack --dry-run` shows only expected files
- Zero `: any` or `as any` in src/*.ts
</verification>

<success_criteria>
The npm package is verified publish-ready. publint, attw, biome any-audit, tarball contents, and the prepublishOnly pipeline all pass without errors. Phase 7 success criteria from ROADMAP are fully met.
</success_criteria>

<output>
After completion, create `.planning/phases/07-package-validation/07-03-SUMMARY.md`
</output>
