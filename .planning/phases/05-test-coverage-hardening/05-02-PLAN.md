---
phase: 05-test-coverage-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/resources/pedidos.test.ts
  - tests/resources/financeiros.test.ts
  - tests/resources/cadastros.test.ts
  - tests/resources/gateway.test.ts
autonomous: true
requirements: [TEST-01, TEST-06]

must_haves:
  truths:
    - "Unit tests exist for PedidosResource covering consultar, consultarTodos, criar, atualizar, cancelar, confirmar, faturar, incluirNotaGateway, incluirAlterarItem, excluirItem"
    - "Unit tests exist for FinanceirosResource covering all 12+ methods"
    - "Unit tests exist for CadastrosResource covering REST methods, iterators, and Gateway methods (tiposNegociacao, modelosNota)"
    - "Unit tests exist for GatewayResource covering loadRecords, loadRecord, saveRecord"
    - "TAXAJURO empty object edge case is tested in cadastros tiposNegociacao test"
  artifacts:
    - path: "tests/resources/pedidos.test.ts"
      provides: "PedidosResource unit tests including serialize() paths"
      min_lines: 120
    - path: "tests/resources/financeiros.test.ts"
      provides: "FinanceirosResource unit tests"
      min_lines: 100
    - path: "tests/resources/cadastros.test.ts"
      provides: "CadastrosResource unit tests including Gateway methods"
      min_lines: 100
    - path: "tests/resources/gateway.test.ts"
      provides: "GatewayResource unit tests"
      min_lines: 50
  key_links:
    - from: "tests/resources/pedidos.test.ts"
      to: "src/resources/pedidos.ts"
      via: "import PedidosResource"
      pattern: "import.*PedidosResource.*from"
    - from: "tests/resources/cadastros.test.ts"
      to: "src/resources/cadastros.ts"
      via: "import CadastrosResource"
      pattern: "import.*CadastrosResource.*from"
    - from: "tests/resources/gateway.test.ts"
      to: "src/resources/gateway.ts"
      via: "import GatewayResource"
      pattern: "import.*GatewayResource.*from"
---

<objective>
Write unit tests for the 4 complex resources: pedidos (REST + Gateway + serialize), financeiros (many methods), cadastros (REST + Gateway + deserializeRows), and gateway (generic CRUD).

Purpose: Cover ~570 lines of resource code that involves both REST and Gateway protocols, serialize/deserialize calls, and numerous optional parameters. These are the most complex resources and need their own plan to stay within context budget.

Output: 4 new test files in tests/resources/ covering all public methods with mock HttpClient.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-test-coverage-hardening/05-RESEARCH.md
</context>

<interfaces>
<!-- Gateway response format for deserializeRows (used by cadastros, gateway) -->
```typescript
// Mock gatewayCall must return this structure for loadRecords:
{
  entities: {
    total: '2',
    hasMoreResult: 'false',
    offsetPage: '0',
    metadata: {
      fields: {
        field: [{ name: 'FIELD1' }, { name: 'FIELD2' }],
      },
    },
    entity: [
      { f0: { $: 'val1' }, f1: { $: 'val2' } },
    ],
  },
}
```

<!-- serialize() wraps values as { $: value } for Gateway input -->
```typescript
// PedidosResource uses serialize() on input before gatewayCall
// Mock should verify gatewayCall receives the serialized structure
```

<!-- PedidosResource methods that use gatewayCall -->
```typescript
confirmar(input: ConfirmarPedidoInput, options?: RequestOptions): Promise<void>
faturar(input: FaturarPedidoInput, options?: RequestOptions): Promise<void>
incluirNotaGateway(input: IncluirNotaGatewayInput, options?: RequestOptions): Promise<{ codigoPedido: number }>
incluirAlterarItem(codigoPedido: number, itens: ItemNotaGatewayInput[], options?: RequestOptions): Promise<void>
excluirItem(codigoPedido: number, sequencia: number, options?: RequestOptions): Promise<void>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for pedidos and financeiros resources</name>
  <files>
    tests/resources/pedidos.test.ts
    tests/resources/financeiros.test.ts
  </files>
  <read_first>
    src/resources/pedidos.ts
    src/resources/financeiros.ts
    tests/resources/pedidos-idempotency.test.ts
    src/core/gateway-serializer.ts
    src/types/pedidos.ts
    src/types/financeiros.ts
  </read_first>
  <action>
Create unit tests for PedidosResource and FinanceirosResource using createMockHttp() pattern.

**pedidos.test.ts** -- test all public methods (the existing pedidos-idempotency.test.ts only tests idempotency key forwarding, NOT the actual method logic):

REST methods:
- consultar() calls restGet with '/vendas/pedidos' and query params including codigoEmpresa (required), page (default 0)
- consultar() with ALL optional params (modifiedSince, codigoNota, numeroNota, serieNota, dataNegociacaoInicio, dataNegociacaoFinal, codigoCliente, confirmada, pendente, codigoNatureza, codigoCentroResultado, codigoProjeto, codigoOrdemCarga) -- verify each is converted to string and added to query
- consultarTodos() returns AsyncGenerator
- criar() calls restPost with '/vendas/pedidos' and input
- atualizar() calls restPut with '/vendas/pedidos/{id}' and input
- cancelar() calls restPost with '/vendas/pedidos/{id}/cancela' with motivo

Gateway methods:
- confirmar() calls gatewayCall with 'mgecom', 'ServicosNfeSP.confirmarNota', and NUNOTA wrapped in {$: string}
- confirmar() with compensarAutomaticamente=true includes COMPENSAR: { $: 'S' }
- confirmar() without compensarAutomaticamente omits COMPENSAR
- faturar() calls gatewayCall with correct structure including tipoFaturamento default 'FaturamentoNormal' and faturarTodosItens default true
- incluirNotaGateway() serializes cabecalho and itens, calls gatewayCall, returns { codigoPedido } extracted from NUNOTA. Mock gatewayCall to return { NUNOTA: 42 }
- incluirAlterarItem() serializes itens and calls gatewayCall with NUNOTA
- excluirItem() calls gatewayCall with NUNOTA and SEQUENCIA

**financeiros.test.ts** -- test all public methods:

Read src/resources/financeiros.ts first to discover all method signatures. Test each method:
- listarTiposPagamento() -- restGet with correct path
- buscarTipoPagamento() -- restGet with id in path
- listarReceitas() / registrarReceita() / atualizarReceita() / baixarReceita()
- listarDespesas() / registrarDespesa() / atualizarDespesa() / baixarDespesa()
- listarMoedas() / buscarMoeda()
- listarContasBancarias() / buscarContaBancaria()
- All listarTodos*() iterators return AsyncGenerator

For each REST method: verify correct path, query params, and that response is properly extracted via extractRestData + normalizeRestPagination.

Use Biome-compliant style: single quotes, 100 char line width, 2-space indent, trailing commas.
  </action>
  <verify>
    <automated>npx vitest run tests/resources/pedidos.test.ts tests/resources/financeiros.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -l "PedidosResource" tests/resources/pedidos.test.ts returns match
    - grep -l "confirmar\|faturar\|incluirNotaGateway" tests/resources/pedidos.test.ts returns match
    - grep -l "FinanceirosResource" tests/resources/financeiros.test.ts returns match
    - grep -c "it\b\|test\b" tests/resources/pedidos.test.ts shows >= 10 test cases
    - grep -c "it\b\|test\b" tests/resources/financeiros.test.ts shows >= 10 test cases
    - npx vitest run tests/resources/pedidos.test.ts exits 0
    - npx vitest run tests/resources/financeiros.test.ts exits 0
  </acceptance_criteria>
  <done>pedidos.test.ts covers all 10 PedidosResource methods including Gateway methods with serialize(). financeiros.test.ts covers all FinanceirosResource methods. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for cadastros and gateway resources</name>
  <files>
    tests/resources/cadastros.test.ts
    tests/resources/gateway.test.ts
  </files>
  <read_first>
    src/resources/cadastros.ts
    src/resources/gateway.ts
    src/core/gateway-serializer.ts
    src/types/cadastros.ts
  </read_first>
  <action>
Create unit tests for CadastrosResource and GatewayResource.

**cadastros.test.ts** -- test all 13+ public methods:

REST methods (each follows the same extractRestData pattern):
- listarTiposOperacao() with default page and with tipoMovimento param
- buscarTipoOperacao() with id in path
- listarNaturezas() / buscarNatureza()
- listarProjetos() / buscarProjeto()
- listarCentrosResultado() / buscarCentroResultado()
- listarEmpresas() / buscarEmpresa()
- listarUsuarios() -- returns array directly, not PaginatedResult

All iterators: listarTodosTiposOperacao(), listarTodasNaturezas(), listarTodosProjetos(), listarTodosCentrosResultado(), listarTodasEmpresas() -- each returns AsyncGenerator

Gateway methods (use deserializeRows):
- listarTiposNegociacao() -- mock gatewayCall to return entities structure with metadata/entity format. Verify it calls gatewayCall with 'mge', 'CRUDServiceProvider.loadRecords', and rootEntity='TipoNegociacao'. Verify response is mapped to { codigoTipoNegociacao, descricao, taxaJuro, ativo }
- listarTiposNegociacao({ apenasAtivos: false }) uses criteria '1 = 1' instead of "this.ATIVO = 'S'"
- **TEST-06 TAXAJURO edge case**: listarTiposNegociacao() with TAXAJURO field containing `{ $: {} }` (empty object) returns taxaJuro: 0, not NaN or "[object Object]"
- listarModelosNota() -- mock with entities structure, verify mapping to { numeroModelo, descricao, codigoTipoOperacao, codigoTipoNegociacao, codigoEmpresa, codigoNatureza, codigoCentroResultado }
- listarModelosNota() with optional fields (CODNAT, CODCENCUS) absent returns undefined for those fields

Gateway mock response structure:
```typescript
{
  entities: {
    total: '2', hasMoreResult: 'false', offsetPage: '0',
    metadata: { fields: { field: [{ name: 'CODTIPVENDA' }, { name: 'DESCRTIPVENDA' }, { name: 'TAXAJURO' }, { name: 'ATIVO' }] } },
    entity: [
      { f0: { $: '1' }, f1: { $: 'A VISTA' }, f2: { $: '0' }, f3: { $: 'S' } },
      { f0: { $: '2' }, f1: { $: 'PARCELADO' }, f2: { $: {} }, f3: { $: 'S' } },
    ],
  },
}
```

**gateway.test.ts** -- test all 3 public methods:

Read src/resources/gateway.ts to understand exact method signatures. Test:
- loadRecords() calls gatewayCall with correct serviceName and structure, returns deserialized rows
- loadRecord() calls gatewayCall for single record
- saveRecord() serializes input and calls gatewayCall

Use Biome-compliant style: single quotes, 100 char line width, 2-space indent, trailing commas.
  </action>
  <verify>
    <automated>npx vitest run tests/resources/cadastros.test.ts tests/resources/gateway.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -l "CadastrosResource" tests/resources/cadastros.test.ts returns match
    - grep -l "tiposNegociacao\|listarTiposNegociacao" tests/resources/cadastros.test.ts returns match
    - grep -l "TAXAJURO\|taxaJuro.*0" tests/resources/cadastros.test.ts returns match (TEST-06)
    - grep -l "GatewayResource" tests/resources/gateway.test.ts returns match
    - grep -c "it\b\|test\b" tests/resources/cadastros.test.ts shows >= 12 test cases
    - npx vitest run tests/resources/cadastros.test.ts exits 0
    - npx vitest run tests/resources/gateway.test.ts exits 0
  </acceptance_criteria>
  <done>cadastros.test.ts covers all REST methods, all iterators, and both Gateway methods (tiposNegociacao with TAXAJURO edge case, modelosNota with optional fields). gateway.test.ts covers loadRecords, loadRecord, saveRecord. All tests pass.</done>
</task>

</tasks>

<verification>
All 4 test files pass: `npx vitest run tests/resources/pedidos.test.ts tests/resources/financeiros.test.ts tests/resources/cadastros.test.ts tests/resources/gateway.test.ts --reporter=verbose`
TAXAJURO empty object edge case tested in cadastros
</verification>

<success_criteria>
- 4 new test files created in tests/resources/
- All tests pass green
- PedidosResource: all 10 methods tested including Gateway + serialize paths
- FinanceirosResource: all 12+ methods tested
- CadastrosResource: all REST + Gateway methods tested with TAXAJURO edge case
- GatewayResource: all 3 methods tested
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-coverage-hardening/05-02-SUMMARY.md`
</output>
