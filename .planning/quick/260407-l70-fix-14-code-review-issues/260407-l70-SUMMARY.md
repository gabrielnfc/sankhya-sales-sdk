---
type: quick
description: Fix 14 code review issues for v1.0.0 grouped by severity
tasks_completed: 3
tasks_total: 3
duration: 5m47s
completed: 2026-04-07T18:25:12Z
commits:
  - hash: 2dfe40a
    message: "fix(260407-l70): critical fixes — wire withRetry, escape PK injection, Node 22 LTS"
  - hash: bc500a7
    message: "fix(260407-l70): important fixes — typed returns, private getters, auth timeout, 401 token check, Despesa type"
  - hash: 4ef7891
    message: "fix(260407-l70): suggestions — restDelete, loadEnv quotes, coverage exclude, prepublishOnly, secrets guard"
key_files_modified:
  - src/core/http.ts
  - src/core/auth.ts
  - src/client.ts
  - src/resources/gateway.ts
  - src/resources/financeiros.ts
  - src/resources/fiscal.ts
  - src/types/financeiros.ts
  - src/types/fiscal.ts
  - src/types/index.ts
  - src/index.ts
  - vitest.config.ts
  - package.json
  - .github/workflows/release.yml
  - .github/workflows/integration.yml
  - tests/resources/client.test.ts
  - tests/resources/gateway.test.ts
  - tests/core/http.test.ts
---

# Quick Task 260407-l70: Fix 14 Code Review Issues Summary

All 14 code review issues resolved across 3 severity tiers (critical, important, suggestions) to harden SDK before v1.0.0 publish.

## Task 1: CRITICAL fixes (Issues 1-3)

**Issue 1 — Wire withRetry into HttpClient:** Imported `withRetry` from `retry.js`, added `retries` field to constructor, wrapped fetch call in `requestWithRetry` with method-aware retry logic. Updated `SankhyaClient` to pass `config.retries ?? 3`.

**Issue 2 — Fix PK string injection in GatewayResource.loadRecord:** Replaced raw interpolation with single-quote wrapping and `''` escape for embedded quotes. Updated 2 gateway tests to expect quoted format.

**Issue 3 — Node version in release.yml:** Changed from 24 to 22 (current LTS).

## Task 2: IMPORTANT fixes (Issues 4-8)

**Issue 4 — Typed return values:** Added `BaixaResult` interface for `baixarReceita`/`baixarDespesa` (replaces `Promise<unknown>`). Added `ImportNfseResult` for `importarNfse`. Exported both from public API.

**Issue 5 — Remove internal getters from public surface:** Removed `getAuthManager()`, `getLogger()`, `getConfig()` entirely from `SankhyaClient` (unused internally, no external callers). Kept `getHttpClient()` as `@internal` public (needed by integration tests). Removed associated unit tests and the stored `config` field.

**Issue 6 — Auth timeout:** Added 30s `AbortController` timeout to `auth.authenticate()` fetch call with proper cleanup and `AuthError` on abort.

**Issue 7 — 401 token verification:** After invalidation, verifies new token differs from failed token before retrying. Throws `ApiError` if same token returned. Updated 2 unit tests.

**Issue 8 — Despesa type:** Added `Despesa` type alias (`= Receita`) used by `listarDespesas`/`listarTodasDespesas`. Exported from public API.

## Task 3: SUGGESTIONS (Issues 9-14)

**Issue 9 — restDelete:** Added `restDelete<T>` method to `HttpClient`.

**Issue 10 — loadEnv quotes:** Strip surrounding single/double quotes from `.env` values in `vitest.config.ts`.

**Issue 11 — Coverage exclude:** Added `src/resources/index.ts` to coverage exclusion list (barrel file).

**Issue 12 — prepublishOnly:** Simplified to `npm run build && npx publint` (CI handles full validation).

**Issue 13 — README links:** Verified all 18 links exist. No changes needed.

**Issue 14 — Integration secrets guard:** Added `if: ${{ secrets.SANKHYA_BASE_URL != '' }}` to integration job.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated gateway tests for new quoted PK format**
- Found during: Task 1
- Issue: Tests expected unquoted PK values (`this.CODPROD = 100`) but fix now quotes them
- Fix: Updated test expectations to match new format (`this.CODPROD = '100'`)
- Files modified: tests/resources/gateway.test.ts

**2. [Rule 1 - Bug] Updated 401 retry tests for token verification**
- Found during: Task 2
- Issue: Mock auth always returned same token, triggering new same-token check
- Fix: Updated mock to return different tokens; changed second test to verify same-token error
- Files modified: tests/core/http.test.ts

**3. [Rule 2 - Critical] Removed unused config field after removing getConfig()**
- Found during: Task 2
- Issue: `private readonly config` field triggered TS6133 (declared but never read)
- Fix: Removed field and assignment since no internal code uses it
- Files modified: src/client.ts

**4. [Rule 1 - Bug] Fixed noInferrableTypes lint on retries parameter**
- Found during: Task 3
- Issue: `retries: number = 3` triggers Biome noInferrableTypes
- Fix: Changed to `retries = 3` (type inferred)
- Files modified: src/core/http.ts

## Known Stubs

None.

## Verification Results

- `npx tsc --noEmit`: zero errors
- `npx vitest run --exclude 'tests/integration/**'`: 252 tests pass (21 files)
- `npm run build`: builds successfully (ESM + CJS + DTS)
- Biome lint: only pre-existing CRLF formatting issues in worktree (not introduced by this task)

## Self-Check: PASSED
