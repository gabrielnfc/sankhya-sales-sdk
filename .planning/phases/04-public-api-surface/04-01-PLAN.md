---
phase: 04-public-api-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/errors.ts
  - src/types/config.ts
  - src/index.ts
  - tests/core/errors.test.ts
  - tests/api-surface.test.ts
autonomous: true
requirements: [APIS-01, APIS-02, APIS-05]
must_haves:
  truths:
    - "isSankhyaError(err) narrows unknown to SankhyaError in TypeScript"
    - "isAuthError(err), isApiError(err), isGatewayError(err), isTimeoutError(err) narrow to their respective types"
    - "SankhyaErrorCode union type enables exhaustive switch statements"
    - "Internal utilities (withRetry, createPaginator, serialize, deserialize, etc.) are NOT exported from the package entry point"
    - "Public types (SankhyaClient, errors, resources, domain types, RequestOptions) remain exported"
  artifacts:
    - path: "src/core/errors.ts"
      provides: "Type guard functions and SankhyaErrorCode union"
      exports: ["isSankhyaError", "isAuthError", "isApiError", "isGatewayError", "isTimeoutError", "SankhyaErrorCode"]
    - path: "src/types/config.ts"
      provides: "RequestOptions interface for per-call overrides"
      exports: ["RequestOptions"]
    - path: "src/index.ts"
      provides: "Audited public API surface"
    - path: "tests/core/errors.test.ts"
      provides: "Type guard unit tests"
    - path: "tests/api-surface.test.ts"
      provides: "Export audit test"
  key_links:
    - from: "src/index.ts"
      to: "src/core/errors.ts"
      via: "re-export type guards and SankhyaErrorCode"
      pattern: "export.*isSankhyaError|isAuthError|SankhyaErrorCode"
    - from: "src/index.ts"
      to: "src/types/config.ts"
      via: "re-export RequestOptions type"
      pattern: "export type.*RequestOptions"
---

<objective>
Add type guard functions for the error hierarchy, export the SankhyaErrorCode union type, define RequestOptions interface, and audit src/index.ts to remove internal utilities from the public API surface.

Purpose: Establish the public API contracts that downstream plans (02, 03) depend on -- type guards for consumer error handling, RequestOptions for per-call overrides, and a clean export surface.
Output: Updated errors.ts with type guards + union, config.ts with RequestOptions, cleaned index.ts, and tests proving correctness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-public-api-surface/04-RESEARCH.md

@src/core/errors.ts
@src/types/config.ts
@src/index.ts

<interfaces>
<!-- Current error hierarchy in src/core/errors.ts -->
```typescript
export class SankhyaError extends Error {
  readonly code: string;
  readonly statusCode?: number;
  readonly details?: unknown;
}

export class AuthError extends SankhyaError {
  override readonly code = 'AUTH_ERROR' as const;
}

export class ApiError extends SankhyaError {
  override readonly code = 'API_ERROR' as const;
  readonly endpoint: string;
  readonly method: string;
}

export class GatewayError extends SankhyaError {
  override readonly code = 'GATEWAY_ERROR' as const;
  readonly serviceName: string;
  readonly tsErrorCode?: string;
  readonly tsErrorLevel?: string;
}

export class TimeoutError extends SankhyaError {
  override readonly code = 'TIMEOUT_ERROR' as const;
}
```

<!-- Internal utilities currently exported from src/index.ts that MUST be removed (per APIS-05) -->
- createLogger (from ./core/logger.js)
- serialize, deserialize, deserializeRows (from ./core/gateway-serializer.js)
- normalizeRestPagination, normalizeGatewayPagination, extractRestData, createPaginator (from ./core/pagination.js)
- FetchPage type (from ./core/pagination.js)
- withRetry (from ./core/retry.js)
- RetryOptions type (from ./core/retry.js)
- toSankhyaDate, toSankhyaDateTime, toISODate (from ./core/date.js)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add type guards, SankhyaErrorCode union, and RequestOptions interface</name>
  <files>src/core/errors.ts, src/types/config.ts, tests/core/errors.test.ts</files>
  <read_first>src/core/errors.ts, src/types/config.ts, tests/core/errors.test.ts</read_first>
  <behavior>
    - isSankhyaError(new AuthError('x')) returns true
    - isSankhyaError(new Error('x')) returns false
    - isSankhyaError(null) returns false
    - isSankhyaError('string') returns false
    - isAuthError(new AuthError('x')) returns true
    - isAuthError(new ApiError('x','/',''GET')) returns false
    - isApiError(new ApiError('x','/test','GET')) returns true
    - isGatewayError(new GatewayError('x','svc')) returns true
    - isTimeoutError(new TimeoutError('x')) returns true
    - isTimeoutError(new AuthError('x')) returns false
    - SankhyaErrorCode type accepts 'AUTH_ERROR' | 'API_ERROR' | 'GATEWAY_ERROR' | 'TIMEOUT_ERROR'
  </behavior>
  <action>
    1. Add to the BOTTOM of src/core/errors.ts (after TimeoutError class):
       - `export type SankhyaErrorCode = 'AUTH_ERROR' | 'API_ERROR' | 'GATEWAY_ERROR' | 'TIMEOUT_ERROR';` (per APIS-02, use explicit string literal union as recommended in research)
       - Five type guard functions using `instanceof` (per APIS-01):
         - `isSankhyaError(err: unknown): err is SankhyaError`
         - `isAuthError(err: unknown): err is AuthError`
         - `isApiError(err: unknown): err is ApiError`
         - `isGatewayError(err: unknown): err is GatewayError`
         - `isTimeoutError(err: unknown): err is TimeoutError`
       - Each guard: `return err instanceof ClassName;`
    2. Add to src/types/config.ts (per APIS-06 contract, used by Plan 02 and 03):
       ```typescript
       export interface RequestOptions {
         /** Override default timeout for this request (ms) */
         timeout?: number;
         /** External AbortSignal for cancellation */
         signal?: AbortSignal;
         /** Idempotency key for mutation safety (forwarded as X-Idempotency-Key header) */
         idempotencyKey?: string;
       }
       ```
    3. Add tests in tests/core/errors.test.ts in a new describe('type guards') block:
       - Test each guard with correct instance, wrong instance, null, undefined, plain object, and string
       - Test SankhyaErrorCode exhaustive switch compiles (runtime assertion that all 4 codes are covered)
  </action>
  <verify>
    <automated>npx vitest run tests/core/errors.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "isSankhyaError" src/core/errors.ts returns match
    - grep "isAuthError" src/core/errors.ts returns match
    - grep "isApiError" src/core/errors.ts returns match
    - grep "isGatewayError" src/core/errors.ts returns match
    - grep "isTimeoutError" src/core/errors.ts returns match
    - grep "SankhyaErrorCode" src/core/errors.ts returns match
    - grep "RequestOptions" src/types/config.ts returns match
    - grep "idempotencyKey" src/types/config.ts returns match
    - grep "type guards" tests/core/errors.test.ts returns match
  </acceptance_criteria>
  <done>All 5 type guards correctly narrow unknown to specific error types; SankhyaErrorCode union type exported; RequestOptions interface defined with timeout, signal, and idempotencyKey fields; all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Audit and clean src/index.ts exports, add export surface test</name>
  <files>src/index.ts, tests/api-surface.test.ts</files>
  <read_first>src/index.ts</read_first>
  <action>
    Per APIS-05, remove ALL internal utility exports from src/index.ts:

    REMOVE these export lines entirely:
    - `export { createLogger } from './core/logger.js';`
    - `export { serialize, deserialize, deserializeRows } from './core/gateway-serializer.js';`
    - `export { normalizeRestPagination, normalizeGatewayPagination, extractRestData, createPaginator } from './core/pagination.js';`
    - `export type { FetchPage } from './core/pagination.js';`
    - `export { withRetry } from './core/retry.js';`
    - `export type { RetryOptions } from './core/retry.js';`
    - `export { toSankhyaDate, toSankhyaDateTime, toISODate } from './core/date.js';`

    ADD these new exports:
    - Type guards: `export { isSankhyaError, isAuthError, isApiError, isGatewayError, isTimeoutError } from './core/errors.js';`
    - SankhyaErrorCode: `export type { SankhyaErrorCode } from './core/errors.js';`
    - RequestOptions: `export type { RequestOptions } from './types/config.js';` (add to the existing type import from config)

    KEEP all existing exports for: SankhyaClient, Resource classes, Error classes, all domain types/interfaces/enums, SankhyaConfig, TokenCacheProvider, LoggerOptions, LogLevel, Logger, PaginatedResult, PaginationParams.

    IMPORTANT: Check all test files for imports from the index barrel that reference removed utilities. Tests must import internal utilities directly from their source files (e.g., `from '../src/core/pagination.js'` not `from '../src/index.js'`). Fix any broken test imports.

    Create tests/api-surface.test.ts:
    - Import * from the src/index.ts barrel
    - Assert that these names ARE exported: SankhyaClient, isSankhyaError, isAuthError, isApiError, isGatewayError, isTimeoutError, SankhyaError, AuthError, ApiError, GatewayError, TimeoutError, plus all 10 Resource classes
    - Assert that these names are NOT exported: createLogger, serialize, deserialize, deserializeRows, normalizeRestPagination, normalizeGatewayPagination, extractRestData, createPaginator, withRetry, toSankhyaDate, toSankhyaDateTime, toISODate
    - Use `expect(exports.isSankhyaError).toBeDefined()` and `expect((exports as Record<string,unknown>).createLogger).toBeUndefined()` patterns
  </action>
  <verify>
    <automated>npx vitest run tests/api-surface.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "createLogger" src/index.ts returns NO match
    - grep "serialize" src/index.ts returns NO match (except in comments)
    - grep "deserialize" src/index.ts returns NO match
    - grep "createPaginator" src/index.ts returns NO match
    - grep "withRetry" src/index.ts returns NO match
    - grep "toSankhyaDate" src/index.ts returns NO match
    - grep "isSankhyaError" src/index.ts returns match
    - grep "SankhyaErrorCode" src/index.ts returns match
    - grep "RequestOptions" src/index.ts returns match
  </acceptance_criteria>
  <done>Internal utilities removed from public exports; type guards, SankhyaErrorCode, and RequestOptions added to public exports; api-surface test validates the export boundary; all existing tests still pass</done>
</task>

</tasks>

<verification>
npx vitest run tests/core/errors.test.ts tests/api-surface.test.ts
npx vitest run
</verification>

<success_criteria>
- 5 type guard functions exported from src/core/errors.ts and re-exported from src/index.ts
- SankhyaErrorCode union type exported
- RequestOptions interface defined in src/types/config.ts with timeout, signal, idempotencyKey
- 14 internal utilities removed from src/index.ts
- api-surface.test.ts validates the export boundary
- All tests pass (existing + new)
</success_criteria>

<output>
After completion, create `.planning/phases/04-public-api-surface/04-01-SUMMARY.md`
</output>
