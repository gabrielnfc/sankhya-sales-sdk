---
phase: 06-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - typedoc.json
  - package.json
  - .gitignore
  - CHANGELOG.md
autonomous: true
requirements:
  - DOCS-03
  - DOCS-06

must_haves:
  truths:
    - "npm run docs generates HTML API reference in docs/api/ without errors"
    - "CHANGELOG.md exists with v0.1.0 entry in Keep a Changelog format"
    - "docs/api/ is excluded from git tracking"
  artifacts:
    - path: "typedoc.json"
      provides: "TypeDoc configuration"
      contains: "entryPoints"
    - path: "CHANGELOG.md"
      provides: "Project changelog"
      contains: "## [0.1.0]"
  key_links:
    - from: "package.json"
      to: "typedoc"
      via: "docs script"
      pattern: '"docs":\\s*"typedoc"'
    - from: "typedoc.json"
      to: "src/index.ts"
      via: "entryPoints config"
      pattern: '"src/index.ts"'
---

<objective>
Set up TypeDoc for API reference generation and create CHANGELOG.md.

Purpose: Establish the documentation toolchain (TypeDoc) so TSDoc annotations added in Plan 02 will produce navigable HTML output, and create the changelog required for v1.0.0.
Output: Working `npm run docs` command, typedoc.json config, CHANGELOG.md with v0.1.0 entry.
</objective>

<execution_context>
@C:/Users/gabri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gabri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install TypeDoc and configure API reference generation</name>
  <files>package.json, typedoc.json, .gitignore</files>
  <read_first>
    - package.json (current scripts and devDependencies)
    - .gitignore (current exclusions)
    - src/index.ts (barrel file used as TypeDoc entryPoint)
    - tsconfig.json (TypeDoc reads this for resolution)
  </read_first>
  <action>
1. Install TypeDoc as devDependency:
   ```
   npm install -D typedoc@^0.28.18
   ```

2. Create `typedoc.json` at project root with this exact content:
   ```json
   {
     "$schema": "https://typedoc.org/schema.json",
     "entryPoints": ["src/index.ts"],
     "out": "docs/api",
     "name": "sankhya-sales-sdk",
     "readme": "none",
     "excludePrivate": true,
     "excludeInternal": true,
     "excludeExternals": true,
     "categorizeByGroup": true,
     "navigationLinks": {
       "GitHub": "https://github.com/gabrielnfc/sankhya-sales-sdk",
       "npm": "https://www.npmjs.com/package/sankhya-sales-sdk"
     }
   }
   ```

3. Add `"docs": "typedoc"` script to package.json scripts section (after the existing `"dev"` script).

4. Append `docs/api/` to .gitignore (generated HTML must not be committed).

5. Run `npm run docs` to verify TypeDoc generates without fatal errors. Some warnings about missing TSDoc are expected at this stage -- the important thing is that it completes and produces `docs/api/index.html`.
  </action>
  <verify>
    <automated>cd "C:/Users/gabri/OneDrive/Desktop/projetos_true/sankhya_kit" && npm run docs && test -f docs/api/index.html && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - typedoc.json contains `"entryPoints": ["src/index.ts"]`
    - typedoc.json contains `"excludeInternal": true`
    - typedoc.json contains `"out": "docs/api"`
    - package.json scripts section contains `"docs": "typedoc"`
    - package.json devDependencies contains `"typedoc": "^0.28.`
    - .gitignore contains `docs/api/`
    - `docs/api/index.html` file is generated after running `npm run docs`
  </acceptance_criteria>
  <done>TypeDoc installed, configured, and generating HTML API reference from src/index.ts barrel file. Generated docs excluded from git.</done>
</task>

<task type="auto">
  <name>Task 2: Create CHANGELOG.md with v0.1.0 entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - package.json (verify current version is 0.1.0)
    - src/index.ts (verify exported modules for changelog accuracy)
    - src/core/errors.ts (verify error class names for changelog)
  </read_first>
  <action>
Create `CHANGELOG.md` at project root following Keep a Changelog format (https://keepachangelog.com/en/1.1.0/). Use this exact content:

```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [0.1.0] - 2026-04-07

### Added
- `SankhyaClient` facade with lazy-loaded resource access
- OAuth 2.0 authentication with automatic token refresh and caching
- 10 resource modules: clientes, vendedores, produtos, precos, estoque, pedidos, financeiros, cadastros, fiscal, gateway
- 67 API operations (55 REST v1 + 12 Gateway)
- Typed error hierarchy: `SankhyaError`, `AuthError`, `ApiError`, `GatewayError`, `TimeoutError`
- Type guards: `isSankhyaError()`, `isAuthError()`, `isApiError()`, `isGatewayError()`, `isTimeoutError()`
- `SankhyaErrorCode` union type for exhaustive error code matching
- `AsyncGenerator` pagination via `listarTodos()` on all list resources
- Retry with exponential backoff and full jitter for transient failures
- Gateway serializer handling Sankhya `{$: value}` format including TAXAJURO edge case
- Dual ESM/CJS output with full TypeScript declarations (`.d.ts` + `.d.cts`)
- Per-call timeout override via `RequestOptions`
- Idempotency key support for pedidos and financeiros mutations
- Zero runtime dependencies (native fetch, Node 20+)

[Unreleased]: https://github.com/gabrielnfc/sankhya-sales-sdk/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/gabrielnfc/sankhya-sales-sdk/releases/tag/v0.1.0
```
  </action>
  <verify>
    <automated>cd "C:/Users/gabri/OneDrive/Desktop/projetos_true/sankhya_kit" && test -f CHANGELOG.md && grep -q "## \[0.1.0\]" CHANGELOG.md && grep -q "Keep a Changelog" CHANGELOG.md && grep -q "SankhyaClient" CHANGELOG.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md exists at project root
    - CHANGELOG.md contains `## [Unreleased]`
    - CHANGELOG.md contains `## [0.1.0] - 2026-04-07`
    - CHANGELOG.md contains `### Added` section with feature list
    - CHANGELOG.md contains `Keep a Changelog` format header
    - CHANGELOG.md contains link references at bottom (`[Unreleased]:` and `[0.1.0]:`)
    - CHANGELOG.md mentions all 10 resource modules
    - CHANGELOG.md mentions error hierarchy (5 error classes)
  </acceptance_criteria>
  <done>CHANGELOG.md exists with v0.1.0 entry listing all SDK features in Keep a Changelog format.</done>
</task>

</tasks>

<verification>
- `npm run docs` completes without fatal errors and produces `docs/api/index.html`
- `test -f CHANGELOG.md` passes
- `grep -c "entryPoints" typedoc.json` returns 1
- `grep -c "docs/api/" .gitignore` returns at least 1
</verification>

<success_criteria>
TypeDoc toolchain is operational (install + config + script + gitignore) and CHANGELOG.md is created with complete v0.1.0 entry. The `npm run docs` command generates navigable HTML in docs/api/.
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation/06-01-SUMMARY.md`
</output>
