# Paginação 1.5.0 — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Eliminar a perda silenciosa de dados na camada de paginação do SDK e entregar o sinal `degraded` sem alterar fluxo de controle de nenhum consumidor.

**Architecture:** Cada call site passa a declarar explicitamente o contrato do endpoint que consome (`resourceKey`, `contract`, `expectPagination`), porque a API do Sankhya expõe três contratos REST incompatíveis sob uma fachada única e o SDK hoje adivinha qual é. Com o contrato declarado, a extração vira determinística: chave conhecida, resultado único normalizado para array, ausência de metadado esperado vira sinal em vez de lista vazia. O paginador deixa de derivar a próxima página do echo do servidor e passa a usar contador local.

**Tech Stack:** TypeScript 5.8 strict (zero `any`), Vitest 3, Biome 1.9, tsup (ESM+CJS), Node 20+, zero dependências de runtime.

**Spec:** `docs/superpowers/specs/2026-08-29-paginacao-degradada-design.md`

## Global Constraints

- **Runtime:** Node >= 20.0.0. Sem polyfills, `fetch` nativo.
- **Zero dependências de runtime.** Apenas devDependencies.
- **Zero `any`.** `biome.json` define `noExplicitAny: "error"`. `npm run typecheck` precisa passar limpo.
- **Imports com extensão `.js`** mesmo em arquivos `.ts` — `from './pagination.js'`.
- **Biome:** largura 100, aspas simples, vírgula final em tudo. Rode `npm run lint:fix` antes de cada commit.
- **Cobertura alvo:** >= 90%.
- **Nenhuma mudança incompatível de tipo público.** `RestPagination` não pode ser alterada — está exportada em `src/index.ts:67` e alargá-la quebra consumidor (provado com `tsc --strict`, ver spec D3).
- **Nenhuma exceção nova** nesta release. Degradação só marca flag e loga.
- **Comentários e nomes de código em português sem acentuação**, seguindo o padrão do repositório.

---

## Mapa de arquivos

**Criar:**
- `src/types/pagination-contracts.ts` — tipos dos três contratos REST e o descritor de endpoint. Arquivo próprio porque é vocabulário compartilhado entre `core/` e todos os `resources/`, e misturá-lo em `common.ts` engordaria um arquivo que já é o mais importado do projeto.
- `tests/core/pagination-descriptor.test.ts` — tabela de decisão da extração.
- `tests/core/pagination-paginator.test.ts` — iteração, parada, `onDegraded`, aviso de volume.
- `tests/types/rest-pagination-estabilidade.ts` — fixture de consumidor que prova que `RestPagination` não mudou. Sem `.test.` no nome: é checada por `typecheck:tests`, não coletada pelo Vitest.
- `tsconfig.tests.json` — typecheck que inclui `tests/`; o `tsconfig.json` do projeto exclui esse diretório.
- `docs/superpowers/specs/2026-08-29-medicoes-complementares.md` — resultado das sondas da Task 1.

**Modificar:**
- `src/core/pagination.ts` — extração com descritor, normalização por contrato, paginador com contador local.
- `src/core/gateway-serializer.ts:168` — consistência de `totalRecords`.
- `src/types/common.ts` — `PaginatedResult.degraded`, JSDoc de `modifiedSince`.
- `src/types/config.ts` — `SankhyaConfig.onDegradedResponse`.
- `src/resources/*.ts` — 11 arquivos, threading do descritor.
- `src/resources/clientes.ts:71,185` — base de página.
- `CHANGELOG.md`, `package.json` — release.

**Testes existentes que já esperam o comportamento correto:**
- `tests/core/pagination-contracts.test.ts` — 6 casos com `it.fails`
- `tests/resources/clientes-pagination.test.ts` — 3 casos com `it.fails`

Quando a correção entrar, esses `it.fails` passam a falhar. Cada task diz quais virar para `it`.

---

## Task 1: Fechar as lacunas de medição

O spec §10 lista o que ainda não foi medido. Classificar um endpoint errado gera falso-positivo de degradação num recurso inteiro, então **nada de código antes disto**.

**Files:**
- Create: `docs/superpowers/specs/2026-08-29-medicoes-complementares.md`
- Modify: `docs/superpowers/specs/2026-08-29-paginacao-degradada-design.md` (§10)

**Interfaces:**
- Consumes: nada.
- Produces: a tabela de classificação que a Task 3 usa literalmente — para cada call site de `extractRestData`, o trio `resourceKey` / `contract` / `expectPagination`.

- [ ] **Step 1: Escrever a sonda**

Crie `/tmp/probe-lacunas.mjs`. Requer `.env` na raiz com `SANKHYA_BASE_URL`, `SANKHYA_CLIENT_ID`, `SANKHYA_CLIENT_SECRET`, `SANKHYA_X_TOKEN`.

```js
import { readFileSync } from 'node:fs';

function loadEnv(path) {
  const out = {};
  for (const rawLine of readFileSync(path, 'utf8').split('\n')) {
    const line = rawLine.replace(/\r$/, '').trim();      // CRLF quebra o auth: 401 invalid_client
    if (!line || line.startsWith('#')) continue;
    const eq = line.indexOf('=');
    if (eq === -1) continue;
    out[line.slice(0, eq).trim()] = line.slice(eq + 1).trim().replace(/^["']|["']$/g, '');
  }
  return out;
}

const env = loadEnv('.env');
const BASE = env.SANKHYA_BASE_URL.replace(/\/$/, '');
const X = env.SANKHYA_X_TOKEN;

const auth = await fetch(`${BASE}/authenticate`, {
  method: 'POST',
  headers: { 'Content-Type': 'application/x-www-form-urlencoded', 'X-Token': X },
  body: new URLSearchParams({
    grant_type: 'client_credentials',
    client_id: env.SANKHYA_CLIENT_ID,
    client_secret: env.SANKHYA_CLIENT_SECRET,
  }).toString(),
});
const TOKEN = (await auth.json()).access_token;

async function get(path, params) {
  const url = new URL(`${BASE}/v1${path}`);
  for (const [k, v] of Object.entries(params || {})) url.searchParams.set(k, v);
  const res = await fetch(url, {
    headers: { Authorization: `Bearer ${TOKEN}`, 'X-Token': X, Accept: 'application/json' },
  });
  const text = await res.text();
  let json = null;
  try { json = JSON.parse(text); } catch {}
  return { status: res.status, text, json };
}

// LACUNA 1 e 5: endpoints de sub-recurso — chave real e presenca de bloco pagination
const SUB = [
  ['/produtos/10398/componentes', {}],
  ['/produtos/10398/alternativos', {}],
  ['/produtos/10398/volumes', {}],
  ['/estoque/produtos/10398', {}],
  ['/usuarios', { page: '0' }],
  ['/financeiros/contas-bancaria', { page: '0' }],
  ['/precos/produto/10398', { pagina: '1' }],
  ['/vendas/pedidos', { page: '0', codigoEmpresa: '1' }],   // chave real de pedidos.consultar
];

console.log('=== LACUNA 1 e 5: sub-recursos ===');
for (const [path, params] of SUB) {
  const r = await get(path, params);
  if (r.status !== 200 || !r.json) {
    console.log(`${path} -> HTTP ${r.status} :: ${r.text.slice(0, 200)}`);
    continue;
  }
  const keys = Object.keys(r.json);
  const arrayKeys = keys.filter((k) => Array.isArray(r.json[k]));
  const objectKeys = keys.filter(
    (k) => k !== 'pagination' && !Array.isArray(r.json[k]) && typeof r.json[k] === 'object' && r.json[k] !== null,
  );
  console.log(
    `${path} -> chaves=${JSON.stringify(keys)} arrays=${JSON.stringify(arrayKeys)} ` +
      `objetos=${JSON.stringify(objectKeys)} temPagination=${'pagination' in r.json}`,
  );
}

// LACUNA 2: base de pagina dos endpoints REST nao sondados
const BASES = ['/grupos-produto', '/vendedores', '/tipos-operacao', '/naturezas',
               '/centros-resultado', '/empresas', '/usuarios', '/estoque/produtos',
               '/estoque/locais', '/volumes-produtos', '/financeiros/tipos-pagamento',
               '/financeiros/moedas', '/financeiros/receitas'];

console.log('\n=== LACUNA 2: base de pagina (0-based se pagina 0 != pagina 1) ===');
for (const path of BASES) {
  const p0 = await get(path, { page: '0' });
  const p1 = await get(path, { page: '1' });
  const key0 = p0.json ? Object.keys(p0.json).find((k) => Array.isArray(p0.json[k])) : null;
  const key1 = p1.json ? Object.keys(p1.json).find((k) => Array.isArray(p1.json[k])) : null;
  const a = key0 ? JSON.stringify(p0.json[key0][0] ?? null).slice(0, 60) : `HTTP ${p0.status}`;
  const b = key1 ? JSON.stringify(p1.json[key1][0] ?? null).slice(0, 60) : `HTTP ${p1.status}`;
  console.log(`${path}\n   page0: ${a}\n   page1: ${b}\n   => ${a === b ? '1-BASED ou pagina ignorada' : '0-BASED'}`);
}

// LACUNA 3: contrato Gateway — vazio e resultado unico
console.log('\n=== LACUNA 3: Gateway loadRecords ===');
for (const [label, criteria] of [
  ['resultado unico', { expression: 'this.CODPARC = ?', parameters: [{ $: '1' }] }],
  ['resultado vazio', { expression: 'this.CODPARC = ?', parameters: [{ $: '-999999999' }] }],
]) {
  const res = await fetch(`${BASE}/gateway/v1/mge/service.sbr?serviceName=CRUDServiceProvider.loadRecords&outputType=json`, {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${TOKEN}`, 'X-Token': X,
      'Content-Type': 'application/json', Accept: 'application/json',
    },
    body: JSON.stringify({
      serviceName: 'CRUDServiceProvider.loadRecords',
      requestBody: {
        dataSet: {
          rootEntity: 'Parceiro', includePresentationFields: 'N',
          offsetPage: '0',
          criteria,
          entity: { fieldset: { list: 'CODPARC,NOMEPARC' } },
        },
      },
    }),
  });
  const text = await res.text();
  console.log(`${label} -> HTTP ${res.status} :: ${text.slice(0, 400)}`);
}
```

- [ ] **Step 2: Rodar a sonda e guardar a saída**

```bash
node /tmp/probe-lacunas.mjs 2>&1 | tee /tmp/lacunas.txt
```

Se der `401 invalid_client`, o `.env` está com CRLF. Confirme com `file .env`; a sonda já remove `\r`, mas outras ferramentas não.

- [ ] **Step 3: Registrar as medições**

Crie `docs/superpowers/specs/2026-08-29-medicoes-complementares.md` com uma tabela por lacuna. Copie os valores observados — **não** os esperados. A tabela da Lacuna 1 é a entrada da Task 3 e precisa ter uma linha por call site:

```markdown
| Call site | Path | resourceKey | contract | expectPagination |
|---|---|---|---|---|
| `produtos.componentes` | `/produtos/{id}/componentes` | <observado> | rest | <observado> |
```

Se um endpoint devolver erro (produto inexistente no sandbox, permissão), registre `NÃO MEDIDO` com o status HTTP. **Não preencha por analogia** — a Task 3 trata não-medido como bloqueio, não como suposição.

- [ ] **Step 4: Atualizar o spec**

Em `docs/superpowers/specs/2026-08-29-paginacao-degradada-design.md` §10, marque cada lacuma fechada com link para o documento de medições. Lacunas que permanecerem abertas ficam listadas com o motivo.

- [ ] **Step 5: Commit**

```bash
git add docs/superpowers/specs/
git commit -m "docs(spec): medir lacunas de classificação antes de implementar"
```

---

## Task 2: Tipos dos contratos e do sinal de degradação

Só tipos. Nenhuma mudança de comportamento, nenhum teste de runtime falhando.

**Files:**
- Create: `src/types/pagination-contracts.ts`
- Create: `tests/types/rest-pagination-estabilidade.ts`
- Create: `tsconfig.tests.json`
- Modify: `src/types/common.ts`, `src/types/config.ts`, `src/types/index.ts`, `src/index.ts`, `package.json`

**Interfaces:**
- Consumes: nada.
- Produces: `PaginationContract`, `ResourceDescriptor`, `FinanceiroPagination`, `PrecosPagination`, `DegradedInfo`, `PaginatedResult.degraded`, `SankhyaConfig.onDegradedResponse`. Tasks 3 a 9 dependem destes nomes exatos.

- [ ] **Step 1: Criar um typecheck que enxergue os testes**

`tsconfig.json` tem `"exclude": [..., "tests"]`, então `npm run typecheck` **não compila nada dentro de `tests/`**. Sem isto, um arquivo de fixture de tipo seria letra morta: nem o `tsc` nem o Vitest (que só coleta `tests/**/*.test.ts` e, via esbuild, apaga tipos sem checá-los) o avaliariam.

Crie `tsconfig.tests.json`:

```json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": "."
  },
  "include": ["src", "tests"],
  "exclude": ["node_modules", "dist"]
}
```

E em `package.json`, ao lado de `typecheck`:

```json
    "typecheck:tests": "tsc --noEmit -p tsconfig.tests.json",
```

- [ ] **Step 2: Escrever a fixture de estabilidade de tipo**

Crie `tests/types/rest-pagination-estabilidade.ts` — sem `.test.` no nome, porque não é um teste de runtime e o Vitest não deve coletá-lo. Ele existe para quebrar o `typecheck:tests` se alguém alargar `RestPagination`:

```ts
import type { PaginatedResult, RestPagination } from '../../src/types/common.js';

// Codigo de consumidor real, escrito contra o tipo publicado na 1.4.0.
// Se RestPagination for alargada, estas linhas param de compilar.
export function consumidorRestPagination(p: RestPagination) {
  const total: string = p.total;
  const page: string = p.page;
  const offset: string = p.offset;
  const hasMore: string = p.hasMore;
  return { total, page, offset, hasMore };
}

// Consumidor que constroi PaginatedResult (mock de teste).
// `degraded` precisa ser opcional, senao esta construcao quebra.
export function consumidorConstroiResultado(): PaginatedResult<{ id: number }> {
  return { data: [{ id: 1 }], page: 0, hasMore: false, totalRecords: 1 };
}

// Consumidor que le `degraded` do retorno do SDK.
export function consumidorLeDegraded(r: PaginatedResult<{ id: number }>): boolean {
  return r.degraded === true;
}
```

- [ ] **Step 3: Rodar o typecheck dos testes para ver falhar**

```bash
npm run typecheck:tests
```

Esperado: FAIL em `consumidorLeDegraded` — `Property 'degraded' does not exist on type 'PaginatedResult<...>'`. As outras funções compilam. Se passar limpo, o `include` do `tsconfig.tests.json` está errado — corrija antes de seguir, senão a verificação é teatro.

- [ ] **Step 4: Criar os tipos dos contratos**

Crie `src/types/pagination-contracts.ts`:

```ts
/**
 * Contratos de paginacao expostos pela API Sankhya.
 *
 * A API apresenta tres formatos REST incompativeis sob a mesma fachada.
 * Medidos em 29/08/2026; ver o design em
 * docs/superpowers/specs/2026-08-29-paginacao-degradada-design.md
 */
export type PaginationContract = 'rest' | 'financeiro' | 'precos';

/**
 * Paginacao dos endpoints financeiros (`/financeiros/receitas`,
 * `/financeiros/despesas`). Numeros e booleano, pagina base 1, e `total`
 * e censo real — diferente de todo o resto da API.
 */
export interface FinanceiroPagination {
  page: number;
  pageSize: number;
  total: number;
  totalPages: number;
  hasMore: boolean;
}

/**
 * Paginacao dos endpoints de preco (`/precos/tabela/{t}`,
 * `/precos/produto/{p}`). Nao ha bloco `pagination`: os campos vem na
 * raiz do corpo. Pagina base 1 — `pagina=0` devolve HTTP 400.
 */
export interface PrecosPagination {
  pagina: number;
  numeroRegistros: number;
  temMaisRegistros: boolean;
}

/**
 * Declaracao do contrato de um endpoint, fornecida pelo call site.
 *
 * O SDK nao consegue inferir estes valores: a chave do array varia por
 * endpoint e o formato de paginacao varia por familia de endpoint.
 * Declarar transforma deteccao de degradacao em verificacao
 * deterministica, checada em tempo de compilacao.
 */
export interface ResourceDescriptor {
  /** Chave do corpo que carrega os registros (ex.: `'produtos'`, `'data'`). */
  resourceKey: string;
  /** Formato de paginacao deste endpoint. */
  contract: PaginationContract;
  /** `true` se o endpoint devolve metadado de paginacao. */
  expectPagination: boolean;
}

/** Diagnostico de uma resposta degradada. */
export interface DegradedInfo {
  /** Motivo legivel da classificacao. Sempre presente. */
  reason: string;
  /**
   * Chave declarada que o SDK esperava encontrar. Ausente quando a
   * degradacao foi detectada pelo paginador, que nao conhece o descritor.
   */
  expectedKey?: string | undefined;
  /** Chaves realmente presentes no corpo, quando conhecidas. */
  receivedKeys?: string[] | undefined;
  /** Pagina em que ocorreu, quando aplicavel. */
  page?: number | undefined;
}
```

- [ ] **Step 5: Adicionar `degraded` ao resultado paginado**

Em `src/types/common.ts`, dentro de `PaginatedResult<T>`, após `totalRecords`:

```ts
  /**
   * `true` quando a resposta nao trouxe o formato declarado pelo endpoint
   * e os dados podem estar incompletos.
   *
   * Opcional no tipo para nao quebrar quem constroi `PaginatedResult` em
   * mocks; **sempre presente** no retorno do SDK. Lista vazia legitima
   * vem com `degraded: false`.
   */
  degraded?: boolean | undefined;
```

- [ ] **Step 6: Corrigir o JSDoc do filtro incremental**

Em `src/types/common.ts`, substitua o JSDoc de `ModifiedSinceParams.modifiedSince`:

```ts
/** Parametros de filtro por data de modificacao. */
export interface ModifiedSinceParams {
  /**
   * Data no formato `dd/MM/yyyy` ou `dd/MM/yyyy HH:mm:ss`.
   *
   * **Nao aceita ISO 8601** — a API responde `400 ORA-01861`. O filtro e
   * inclusivo (`>=`). Uma janela sem alteracoes responde `404
   * RESOURCE_NOT_FOUND`, nao lista vazia.
   */
  modifiedSince?: string;
}
```

Aplique o mesmo texto ao JSDoc de `dataHoraAlteracao` em `src/types/clientes.ts:136`, trocando o nome do campo.

- [ ] **Step 7: Adicionar a política de degradação à config**

Em `src/types/config.ts`, dentro de `SankhyaConfig`, após `logger`:

```ts
  /**
   * O que fazer quando uma resposta chega degradada.
   *
   * - `'flag'` (default na 1.5.0): marca `degraded` no resultado e emite
   *   `logger.error`. Nao lanca.
   *
   * O default passa a `'throw'` na 2.0.0.
   */
  onDegradedResponse?: 'flag';
```

O tipo aceita só `'flag'` nesta release: `'throw'` não existe ainda, e declarar um valor não implementado é convite a bug.

- [ ] **Step 8: Exportar os tipos novos**

Em `src/types/index.ts`, acrescente:

```ts
export type {
  PaginationContract,
  FinanceiroPagination,
  PrecosPagination,
  ResourceDescriptor,
  DegradedInfo,
} from './pagination-contracts.js';
```

Em `src/index.ts`, dentro do bloco `export type { ... } from './types/index.js';`, acrescente `PaginationContract`, `FinanceiroPagination`, `PrecosPagination`, `DegradedInfo`. **Não** exporte `ResourceDescriptor` — é vocabulário interno entre `core/` e `resources/`, e exportá-lo criaria compromisso público sobre a forma de um detalhe de implementação.

- [ ] **Step 9: Rodar os dois typechecks e a suíte**

```bash
npm run typecheck && npm run typecheck:tests && npm test
```

Esperado: ambos limpos, 418 testes verdes. Nenhum `it.fails` muda — não houve mudança de comportamento.

- [ ] **Step 10: Commit**

```bash
npm run lint:fix
git add src/ tests/ tsconfig.tests.json package.json
git commit -m "feat(types): tipos por contrato de paginação e sinal de degradação"
```

---

## Task 3: Threading do descritor, sem mudar comportamento

Mudança mecânica e ampla. O objetivo é que `tsc` aponte todo call site; o comportamento fica idêntico.

**Files:**
- Modify: `src/core/pagination.ts`
- Modify: os 11 arquivos em `src/resources/` que chamam `extractRestData`

**Interfaces:**
- Consumes: `ResourceDescriptor` (Task 2); a tabela de classificação (Task 1).
- Produces: `extractRestData<T>(response, descriptor): { data: T[]; degraded: boolean; degradedInfo?: DegradedInfo }` e `normalizePagination<T>(data, response, descriptor, degraded): PaginatedResult<T>`.

- [ ] **Step 1: Trocar a assinatura de `extractRestData`**

Em `src/core/pagination.ts`, substitua a função inteira. Nesta task o corpo continua com a semântica antiga (primeiro array), só que passando pela chave declarada quando ela existe:

```ts
export function extractRestData<T>(
  response: Record<string, unknown>,
  descriptor: ResourceDescriptor,
): { data: T[]; degraded: boolean; degradedInfo?: DegradedInfo } {
  const valor = response[descriptor.resourceKey];
  if (Array.isArray(valor)) {
    return { data: valor as T[], degraded: false };
  }
  // Comportamento antigo preservado nesta task: cai para o primeiro array.
  // A Task 4 substitui isto pela tabela de decisao.
  for (const [key, value] of Object.entries(response)) {
    if (key === 'pagination') continue;
    if (Array.isArray(value)) return { data: value as T[], degraded: false };
  }
  return { data: [], degraded: false };
}
```

- [ ] **Step 2: Introduzir `normalizePagination`**

Ainda em `src/core/pagination.ts`, acrescente. `normalizeRestPagination` permanece intacta nesta task — a Task 5 a substitui:

```ts
export function normalizePagination<T>(
  data: T[],
  response: Record<string, unknown>,
  descriptor: ResourceDescriptor,
  degraded: boolean,
  logger?: Logger,
  degradedInfo?: DegradedInfo,
): PaginatedResult<T> {
  if (degraded && logger) {
    logger.error(
      `Resposta degradada: ${degradedInfo?.reason ?? 'formato inesperado'} ` +
        `(chave esperada "${descriptor.resourceKey}", recebidas: ${degradedInfo?.receivedKeys?.join(', ') ?? 'nenhuma'})`,
    );
  }

  const pagination = response.pagination as RestPagination | undefined;
  const base = normalizeRestPagination(data, pagination);
  return { ...base, degraded };
}
```

A assinatura já nasce completa — com `logger` e `degradedInfo` — de propósito. Adicioná-los depois obrigaria a tocar os ~20 call sites uma segunda vez. Nesta task o bloco de log é inalcançável, porque `extractRestData` ainda devolve `degraded: false` sempre; a Task 4 é que o ativa. Note que `noUnusedParameters` está ligado no `tsconfig.json`: os dois parâmetros precisam ser usados no corpo desde já, e são.

Nível `error`, não `warn`: o default do SDK é `warn`, então `warn` apareceria — mas degradação é a condição que este trabalho inteiro existe para tornar visível, e precisa sobreviver a quem sobe o nível para `error`.

- [ ] **Step 3: Rodar o typecheck para ver falhar**

```bash
npm run typecheck
```

Esperado: FAIL com `Expected 2 arguments, but got 1` em ~30 chamadas de `extractRestData`. Essa lista é o roteiro do próximo passo.

- [ ] **Step 4: Migrar os call sites**

Para cada arquivo em `src/resources/`, declare os descritores no topo do módulo — junto dos paths, não inline em cada método, para que descritor e endpoint não se desencontrem:

```ts
const DESCRITOR_PRODUTOS: ResourceDescriptor = {
  resourceKey: 'produtos',
  contract: 'rest',
  expectPagination: true,
};
```

E troque cada chamada:

```ts
// antes
const { data, pagination } = extractRestData<Produto>(raw);
return normalizeRestPagination(data, pagination);

// depois
const { data, degraded, degradedInfo } = extractRestData<Produto>(raw, DESCRITOR_PRODUTOS);
return normalizePagination(
  data, raw, DESCRITOR_PRODUTOS, degraded, this.http.getLogger(), degradedInfo,
);
```

Valores medidos, prontos para uso (spec §3.2):

| Arquivo | Método | resourceKey | contract | expectPagination |
|---|---|---|---|---|
| `produtos.ts` | `listar` | `produtos` | rest | true |
| `produtos.ts` | `listarGrupos` | `grupos` | rest | true |
| `produtos.ts` | `listarVolumes` | `volumes` | rest | true |
| `vendedores.ts` | `listar` | `vendedores` | rest | true |
| `clientes.ts` | `listar` | `clientes` | rest | true |
| `estoque.ts` | `listar` | `estoque` | rest | true |
| `estoque.ts` | `listarLocais` | `locais` | rest | true |
| `cadastros.ts` | `listarTiposOperacao` | `data` | rest | true |
| `cadastros.ts` | `listarNaturezas` | `data` | rest | true |
| `cadastros.ts` | `listarProjetos` | `data` | rest | true |
| `cadastros.ts` | `listarCentrosResultado` | `data` | rest | true |
| `cadastros.ts` | `listarEmpresas` | `empresas` | rest | true |
| `financeiros.ts` | `listarTiposPagamento` | `data` | rest | true |
| `financeiros.ts` | `listarMoedas` | `data` | rest | true |
| `financeiros.ts` | `listarReceitas` | `financeiros` | financeiro | true |
| `financeiros.ts` | `listarDespesas` | `financeiros` | financeiro | true |
| `precos.ts` | `porTabela` | `produtos` | precos | false |
| `precos.ts` | `porProduto` | `produtos` | precos | false |
| `precos.ts` | `porProdutoETabela` | `produtos` | precos | false |
| `pedidos.ts` | `consultar` | ver medição da Task 1 | rest | true |

Os call sites de sub-recurso (`produtos.componentes`, `produtos.alternativos`, `produtos.volumes`, `estoque.porProduto`, `cadastros.listarUsuarios`, `financeiros.listarContasBancarias`, `precos.contextualizado`) usam a classificação medida na Task 1. **Se algum ficou como NÃO MEDIDO, pare e meça** — não preencha por analogia.

- [ ] **Step 5: Rodar typecheck e suíte**

```bash
npm run typecheck && npm test
```

Esperado: typecheck limpo, 418 verdes. Nenhum `it.fails` muda de estado — o comportamento é idêntico.

- [ ] **Step 6: Commit**

```bash
npm run lint:fix
git add src/
git commit -m "refactor(pagination): call sites declaram o contrato do endpoint"
```

---

## Task 4: Tabela de decisão da extração

Aqui o comportamento muda. Primeiro `it.fails` vira `it`.

**Files:**
- Modify: `src/core/pagination.ts`
- Create: `tests/core/pagination-descriptor.test.ts`
- Modify: `tests/core/pagination-contracts.test.ts`

**Interfaces:**
- Consumes: `extractRestData` (Task 3), `DegradedInfo` (Task 2).
- Produces: `extractRestData` com a tabela de decisão completa do spec D2.

- [ ] **Step 1: Escrever os testes que faltam**

Crie `tests/core/pagination-descriptor.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { extractRestData } from '../../src/core/pagination.js';
import type { ResourceDescriptor } from '../../src/types/pagination-contracts.js';

const DESCRITOR: ResourceDescriptor = {
  resourceKey: 'produtos',
  contract: 'rest',
  expectPagination: true,
};

const PAGINATION = { page: '0', offset: '0', total: '1', hasMore: 'false' };

describe('extractRestData — tabela de decisao', () => {
  it('array vira data', () => {
    const r = extractRestData<{ id: number }>(
      { produtos: [{ id: 1 }, { id: 2 }], pagination: PAGINATION },
      DESCRITOR,
    );
    expect(r.data).toHaveLength(2);
    expect(r.degraded).toBe(false);
  });

  it('objeto vira array de um elemento', () => {
    const r = extractRestData<{ id: number }>({ produtos: { id: 1 }, pagination: PAGINATION }, DESCRITOR);
    expect(r.data).toEqual([{ id: 1 }]);
    expect(r.degraded).toBe(false);
  });

  it('array vazio e lista vazia legitima', () => {
    const r = extractRestData({ produtos: [], pagination: PAGINATION }, DESCRITOR);
    expect(r.data).toEqual([]);
    expect(r.degraded).toBe(false);
  });

  it('chave ausente e degradado', () => {
    const r = extractRestData({ outraCoisa: [{ id: 1 }], pagination: PAGINATION }, DESCRITOR);
    expect(r.data).toEqual([]);
    expect(r.degraded).toBe(true);
    expect(r.degradedInfo?.expectedKey).toBe('produtos');
    expect(r.degradedInfo?.receivedKeys).toContain('outraCoisa');
  });

  it('chave com null e degradado', () => {
    const r = extractRestData({ produtos: null, pagination: PAGINATION }, DESCRITOR);
    expect(r.degraded).toBe(true);
  });

  it('chave com escalar e degradado', () => {
    const r = extractRestData({ produtos: 'erro inesperado', pagination: PAGINATION }, DESCRITOR);
    expect(r.degraded).toBe(true);
  });

  it('bloco pagination ausente e degradado quando o contrato o declara', () => {
    const r = extractRestData({ produtos: [{ id: 1 }] }, DESCRITOR);
    expect(r.data).toHaveLength(1);
    expect(r.degraded).toBe(true);
    expect(r.degradedInfo?.reason).toContain('pagination');
  });

  it('bloco pagination ausente NAO e degradado no contrato precos', () => {
    const descritorPrecos: ResourceDescriptor = {
      resourceKey: 'produtos',
      contract: 'precos',
      expectPagination: false,
    };
    const r = extractRestData(
      { codigo: '200', pagina: 1, numeroRegistros: 1, temMaisRegistros: false, produtos: [{ id: 1 }] },
      descritorPrecos,
    );
    expect(r.degraded).toBe(false);
  });
});
```

- [ ] **Step 2: Rodar os testes para ver falhar**

```bash
npx vitest run tests/core/pagination-descriptor.test.ts
```

Esperado: FAIL nos casos de degradação — `expected false to be true`. Os casos de array e vazio passam.

- [ ] **Step 3: Implementar a tabela de decisão**

Em `src/core/pagination.ts`, substitua o corpo de `extractRestData`:

```ts
export function extractRestData<T>(
  response: Record<string, unknown>,
  descriptor: ResourceDescriptor,
): { data: T[]; degraded: boolean; degradedInfo?: DegradedInfo } {
  const receivedKeys = Object.keys(response);
  const valor = response[descriptor.resourceKey];

  let data: T[];
  let reason: string | null = null;

  if (Array.isArray(valor)) {
    data = valor as T[];
  } else if (valor !== null && valor !== undefined && typeof valor === 'object') {
    // A API devolve objeto quando exatamente 1 registro corresponde ao filtro.
    data = [valor as T];
  } else {
    data = [];
    reason =
      valor === undefined
        ? `chave "${descriptor.resourceKey}" ausente na resposta`
        : `chave "${descriptor.resourceKey}" nao contem registros (${valor === null ? 'null' : typeof valor})`;
  }

  if (reason === null && descriptor.expectPagination && response.pagination === undefined) {
    reason = 'bloco "pagination" ausente num endpoint que o declara';
  }

  if (reason === null) {
    return { data, degraded: false };
  }

  return {
    data,
    degraded: true,
    degradedInfo: { expectedKey: descriptor.resourceKey, receivedKeys, reason },
  };
}
```

- [ ] **Step 4: Rodar os testes para ver passar**

```bash
npx vitest run tests/core/pagination-descriptor.test.ts
```

Esperado: PASS, 8 testes.

- [ ] **Step 5: Virar o `it.fails` que agora passa**

Em `tests/core/pagination-contracts.test.ts`, o teste `'resultado unico chega como objeto e deve virar array de 1 elemento'` agora passa. Troque `it.fails(` por `it(`.

**Atenção:** aquele teste chama `extractRestData` com um argumento só. Acrescente o descritor:

```ts
const { data } = extractRestData<{ codigoProduto: number }>(resposta, {
  resourceKey: 'produtos',
  contract: 'rest',
  expectPagination: true,
});
```

O mesmo vale para os outros casos do arquivo que chamam `extractRestData` — o typecheck aponta cada um.

- [ ] **Step 6: Rodar a suíte inteira**

```bash
npm run typecheck && npm test
```

Esperado: tudo verde. Se algum `it.fails` falhar com "Expect test to fail", é porque a correção o consertou — vire para `it`.

- [ ] **Step 7: Commit**

```bash
npm run lint:fix
git add src/ tests/
git commit -m "fix(pagination): resultado único vira array e ausência de dado vira sinal"
```

---

## Task 5: Normalização por contrato

**Files:**
- Modify: `src/core/pagination.ts`
- Modify: `tests/core/pagination-contracts.test.ts`

**Interfaces:**
- Consumes: `normalizePagination` (Task 3), tipos de contrato (Task 2).
- Produces: `normalizePagination` reconhecendo os três contratos. `normalizeRestPagination` continua exportada e intacta, para os testes existentes que a exercitam diretamente.

- [ ] **Step 1: Rodar os testes que já existem para confirmar o RED**

```bash
npx vitest run tests/core/pagination-contracts.test.ts
```

Três `it.fails` cobrem esta task: `hasMore` booleano, `temMaisRegistros`, e `total "0"`. Confirme que estão como `it.fails` e verdes (ou seja: falhando de verdade).

- [ ] **Step 2: Escrever os helpers de coerção**

Em `src/core/pagination.ts`, antes de `normalizePagination`:

```ts
/** A API usa `'true'` no contrato REST e `true` nos financeiros. */
function ehVerdadeiro(valor: unknown): boolean {
  return valor === true || valor === 'true';
}

/**
 * Converte para numero preservando o zero.
 *
 * `Number.parseInt('0', 10) || undefined` devolveria `undefined` — era
 * assim que lista legitimamente vazia perdia o total.
 */
function paraNumero(valor: unknown): number | undefined {
  if (typeof valor === 'number') return Number.isFinite(valor) ? valor : undefined;
  if (typeof valor === 'string' && valor.trim() !== '') {
    const n = Number.parseInt(valor, 10);
    return Number.isNaN(n) ? undefined : n;
  }
  return undefined;
}
```

- [ ] **Step 3: Implementar a normalização por contrato**

Substitua `normalizePagination`:

A assinatura **não muda** — foi fixada na Task 3, com `logger` e `degradedInfo`. Aqui só o corpo, depois do bloco de log:

```ts
export function normalizePagination<T>(
  data: T[],
  response: Record<string, unknown>,
  descriptor: ResourceDescriptor,
  degraded: boolean,
  logger?: Logger,
  degradedInfo?: DegradedInfo,
): PaginatedResult<T> {
  if (degraded && logger) {
    logger.error(
      `Resposta degradada: ${degradedInfo?.reason ?? 'formato inesperado'} ` +
        `(chave esperada "${descriptor.resourceKey}", recebidas: ${degradedInfo?.receivedKeys?.join(', ') ?? 'nenhuma'})`,
    );
  }

  if (descriptor.contract === 'precos') {
    // Sem bloco `pagination`: os campos vem na raiz do corpo.
    return {
      data,
      page: paraNumero(response.pagina) ?? 1,
      hasMore: ehVerdadeiro(response.temMaisRegistros),
      totalRecords: paraNumero(response.numeroRegistros) ?? data.length,
      degraded,
    };
  }

  const pagination = response.pagination as Record<string, unknown> | undefined;
  if (!pagination) {
    return { data, page: 0, hasMore: false, totalRecords: data.length, degraded };
  }

  return {
    data,
    page: paraNumero(pagination.page) ?? 0,
    hasMore: ehVerdadeiro(pagination.hasMore),
    totalRecords: paraNumero(pagination.total),
    degraded,
  };
}
```

O contrato `financeiro` não precisa de ramo próprio: `paraNumero` e `ehVerdadeiro` já aceitam número e booleano. Um ramo separado seria código morto.

- [ ] **Step 4: Rodar os testes**

```bash
npx vitest run tests/core/pagination-contracts.test.ts
```

Esperado: FAIL com "Expect test to fail" nos três `it.fails` corrigidos — sinal de que a correção funcionou.

- [ ] **Step 5: Virar os `it.fails` corrigidos**

Em `tests/core/pagination-contracts.test.ts`, troque `it.fails(` por `it(` em:
- `'hasMore booleano true deve ser reconhecido como verdadeiro'`
- `'temMaisRegistros true deve produzir hasMore true'`
- `'total "0" deve normalizar para 0, nao para undefined'` (o do contrato REST)

Os dois primeiros chamam `normalizeRestPagination`; troque por `normalizePagination` com o descritor apropriado. O terceiro passa a chamar `normalizePagination(data, { pagination }, descritorRest, false)`.

O `it.fails` de `deserializeRows` **não** muda aqui — é a Task 8.

- [ ] **Step 6: Rodar a suíte inteira**

```bash
npm run typecheck && npm test
```

- [ ] **Step 7: Commit**

```bash
npm run lint:fix
git add src/ tests/
git commit -m "fix(pagination): reconhecer os três contratos REST na normalização"
```

---

## Task 6: Paginador com contador local

**Files:**
- Modify: `src/core/pagination.ts`
- Create: `tests/core/pagination-paginator.test.ts`
- Modify: `tests/core/pagination-contracts.test.ts`

**Interfaces:**
- Consumes: `PaginatedResult` com `degraded` (Task 2).
- Produces: `createPaginator<T>(fetchFn, startPage?, options?)` com `PaginatorOptions { onDegraded?, logger? }`. As Tasks 7 e 9 repassam essas opções.

- [ ] **Step 1: Escrever os testes**

Crie `tests/core/pagination-paginator.test.ts`:

```ts
import { describe, expect, it, vi } from 'vitest';
import { createPaginator } from '../../src/core/pagination.js';
import type { PaginatedResult } from '../../src/types/common.js';

function pagina<T>(data: T[], hasMore: boolean, echo: number, degraded = false): PaginatedResult<T> {
  return { data, page: echo, hasMore, totalRecords: undefined, degraded };
}

describe('createPaginator', () => {
  it('avanca por contador local, ignorando o echo do servidor', async () => {
    const visitadas: number[] = [];
    const fetchFn = async (page: number) => {
      visitadas.push(page);
      return pagina([{ id: page }], visitadas.length < 3, 0); // echo sempre 0
    };

    const itens = [];
    for await (const item of createPaginator(fetchFn, 1)) itens.push(item);

    expect(visitadas).toEqual([1, 2, 3]);
    expect(itens).toHaveLength(3);
  });

  it('para em pagina vazia com hasMore verdadeiro, sinalizando, sem laco infinito', async () => {
    const visitadas: number[] = [];
    const onDegraded = vi.fn();
    const error = vi.fn();
    const logger = { debug: vi.fn(), info: vi.fn(), warn: vi.fn(), error };
    const fetchFn = async (page: number) => {
      visitadas.push(page);
      return page === 0 ? pagina([{ id: 0 }], true, page) : pagina<{ id: number }>([], true, page);
    };

    const itens = [];
    for await (const item of createPaginator(fetchFn, 0, { onDegraded, logger })) itens.push(item);

    expect(visitadas).toEqual([0, 1]);
    expect(itens).toHaveLength(1);
    expect(onDegraded).toHaveBeenCalledTimes(1);
    expect(error).toHaveBeenCalledTimes(1);
    expect(error.mock.calls[0]?.[0]).toContain('hasMore');
  });

  it('pagina vazia com hasMore falso encerra em silencio — fim normal', async () => {
    const onDegraded = vi.fn();
    const error = vi.fn();
    const logger = { debug: vi.fn(), info: vi.fn(), warn: vi.fn(), error };
    const fetchFn = async (page: number) =>
      page === 0 ? pagina([{ id: 0 }], false, page) : pagina<{ id: number }>([], false, page);

    for await (const _ of createPaginator(fetchFn, 0, { onDegraded, logger })) {
      // consumir
    }

    expect(onDegraded).not.toHaveBeenCalled();
    expect(error).not.toHaveBeenCalled();
  });

  it('chama onDegraded uma vez por pagina degradada', async () => {
    const onDegraded = vi.fn();
    const fetchFn = async (page: number) => pagina([{ id: page }], page < 1, page, true);

    for await (const _ of createPaginator(fetchFn, 0, { onDegraded })) {
      // consumir
    }

    expect(onDegraded).toHaveBeenCalledTimes(2);
    expect(onDegraded.mock.calls[0]?.[0]).toMatchObject({ page: 0 });
  });

  it('avisa uma unica vez ao ultrapassar 100 paginas', async () => {
    const warn = vi.fn();
    const logger = { debug: vi.fn(), info: vi.fn(), warn, error: vi.fn() };
    const fetchFn = async (page: number) => pagina([{ id: page }], page < 150, page);

    for await (const _ of createPaginator(fetchFn, 0, { logger })) {
      // consumir
    }

    expect(warn).toHaveBeenCalledTimes(1);
    expect(warn.mock.calls[0]?.[0]).toContain('100');
  });
});
```

- [ ] **Step 2: Rodar para ver falhar**

```bash
npx vitest run tests/core/pagination-paginator.test.ts
```

Esperado: FAIL. O primeiro caso falha com `[1, 1, 1]`; os de `onDegraded` e `logger` falham porque `createPaginator` ainda não aceita o terceiro argumento (erro de tipo).

- [ ] **Step 3: Implementar**

Em `src/core/pagination.ts`, substitua `createPaginator`:

```ts
/** Numero de paginas a partir do qual uma varredura merece aviso. */
const LIMITE_AVISO_PAGINAS = 100;

export interface PaginatorOptions {
  /** Chamado uma vez por pagina degradada. */
  onDegraded?: ((info: DegradedInfo) => void) | undefined;
  /** Logger para o aviso de varredura longa. */
  logger?: Logger | undefined;
}

export async function* createPaginator<T>(
  fetchFn: FetchPage<T>,
  startPage = 0,
  options?: PaginatorOptions,
): AsyncGenerator<T> {
  let currentPage = startPage;
  let paginasLidas = 0;
  let avisou = false;

  while (true) {
    const result = await fetchFn(currentPage);

    if (result.degraded) {
      options?.onDegraded?.({ reason: 'pagina degradada durante varredura', page: currentPage });
    }

    for (const item of result.data) {
      yield item;
    }

    paginasLidas++;
    if (!avisou && paginasLidas > LIMITE_AVISO_PAGINAS) {
      options?.logger?.warn(
        `Varredura ultrapassou ${LIMITE_AVISO_PAGINAS} paginas — confirme se o filtro esta correto`,
      );
      avisou = true;
    }

    if (!result.hasMore) break;

    // Pagina vazia com hasMore verdadeiro e estado impossivel: ou o
    // servidor mentiu, ou o corpo veio degradado. A parada e preservada da
    // 1.4.0 — sem ela isto seria laco infinito — mas agora sai sinal, em
    // vez do silencio de antes. Na 2.0.0 este caso lanca.
    if (result.data.length === 0) {
      const motivo = 'pagina vazia com hasMore verdadeiro — varredura possivelmente incompleta';
      options?.onDegraded?.({ reason: motivo, page: currentPage });
      options?.logger?.error(motivo);
      break;
    }

    currentPage++;
  }
}
```

Importe `Logger` de `../types/config.js` e `DegradedInfo` de `../types/pagination-contracts.js`.

- [ ] **Step 4: Rodar para ver passar**

```bash
npx vitest run tests/core/pagination-paginator.test.ts
```

Esperado: PASS, 4 testes.

- [ ] **Step 5: Virar o `it.fails` do paginador**

Em `tests/core/pagination-contracts.test.ts`, `'startPage 1 com echo de pagina ausente deve avancar, nao repetir'` agora passa. Troque `it.fails(` por `it(`.

- [ ] **Step 6: Rodar a suíte inteira**

```bash
npm run typecheck && npm test
```

Preste atenção em `tests/stress/memory.test.ts` e `tests/stress/concurrency.test.ts`: exercitam `createPaginator` e podem depender do echo. Se falharem, o teste é que estava codificando o comportamento errado — corrija o teste, não a implementação, e diga isso na mensagem de commit.

- [ ] **Step 7: Commit**

```bash
npm run lint:fix
git add src/ tests/
git commit -m "fix(pagination): iterar por contador local em vez do echo do servidor"
```

---

## Task 7: Base de página de clientes

**Files:**
- Modify: `src/resources/clientes.ts:71,185`
- Modify: `tests/resources/clientes-pagination.test.ts`
- Modify: `tests/resources/clientes.test.ts`

**Interfaces:**
- Consumes: nada além do que já existe.
- Produces: nenhuma interface nova.

- [ ] **Step 1: Confirmar o RED**

```bash
npx vitest run tests/resources/clientes-pagination.test.ts
```

Esperado: 3 testes verdes como `it.fails` — ou seja, falhando de verdade.

- [ ] **Step 2: Corrigir o default de `listar`**

`src/resources/clientes.ts:71`:

```ts
    const query: Record<string, string> = { page: String(params?.page ?? 0) };
```

- [ ] **Step 3: Corrigir o `startPage` da varredura**

`src/resources/clientes.ts:185`:

```ts
    return createPaginator((page) => this.listar({ ...params, page }));
```

O `1` sai: `0` já é o default de `createPaginator`. A Task 9 volta a este método para passar o objeto de opções como terceiro argumento — aí o `0` reaparece explícito. Não é contradição: aqui o argumento some porque estava errado; lá ele volta porque há opções a passar.

- [ ] **Step 4: Rodar os testes**

```bash
npx vitest run tests/resources/clientes-pagination.test.ts
```

Esperado: FAIL com "Expect test to fail" nos três — a correção funcionou.

- [ ] **Step 5: Virar os `it.fails`**

Em `tests/resources/clientes-pagination.test.ts`, troque `it.fails(` por `it(` nos três casos.

- [ ] **Step 6: Corrigir o teste que fixava o comportamento errado**

`tests/resources/clientes.test.ts:24` afirma `expect(http.restGet).toHaveBeenCalledWith('/parceiros/clientes', { page: '1' })`. Esse teste codificava o bug. Troque para `{ page: '0' }` e ajuste o nome do teste para `listar() calls restGet with /parceiros/clientes and default page 0`.

- [ ] **Step 7: Rodar a suíte inteira**

```bash
npm run typecheck && npm test
```

- [ ] **Step 8: Commit**

```bash
npm run lint:fix
git add src/ tests/
git commit -m "fix(clientes): endpoint é 0-based; listar() e listarTodos() pulavam a primeira página"
```

---

## Task 8: Consistência do `totalRecords` no Gateway

**Files:**
- Modify: `src/core/gateway-serializer.ts:168`
- Modify: `tests/core/pagination-contracts.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces: nenhuma interface nova.

- [ ] **Step 1: Confirmar o RED**

```bash
npx vitest run tests/core/pagination-contracts.test.ts -t 'Gateway'
```

- [ ] **Step 2: Corrigir**

Em `src/core/gateway-serializer.ts`, no `return` final de `deserializeRows`, `totalRecords` usa `|| undefined` enquanto o retorno do ramo `!rawEntities` usa `|| 0`. Os dois viram a mesma coisa:

```ts
    totalRecords: paraNumeroGateway(entities.total),
```

E acrescente o helper no topo do arquivo:

```ts
/** Converte preservando o zero — `parseInt('0') || undefined` daria `undefined`. */
function paraNumeroGateway(valor: string | undefined): number | undefined {
  if (valor === undefined || valor.trim() === '') return undefined;
  const n = Number.parseInt(valor, 10);
  return Number.isNaN(n) ? undefined : n;
}
```

Aplique no ramo `!rawEntities` também, trocando `Number.parseInt(entities.total ?? '0', 10) || 0` por `paraNumeroGateway(entities.total) ?? 0`.

- [ ] **Step 3: Rodar e virar o `it.fails`**

```bash
npx vitest run tests/core/pagination-contracts.test.ts
```

Esperado: FAIL com "Expect test to fail" no caso do Gateway. Troque `it.fails(` por `it(`.

- [ ] **Step 4: Rodar a suíte inteira**

```bash
npm run typecheck && npm test
```

- [ ] **Step 5: Commit**

```bash
npm run lint:fix
git add src/ tests/
git commit -m "fix(gateway): totalRecords preserva zero e fica consistente entre os retornos"
```

---

## Task 9: Log de degradação e propagação de `onDegraded`

**Files:**
- Modify: `src/core/http.ts`, `src/client.ts`
- Modify: `src/resources/*.ts` (métodos de varredura)
- Create: `tests/core/pagination-logging.test.ts`

**Interfaces:**
- Consumes: `DegradedInfo`, `PaginatorOptions` (Tasks 2 e 6).
- Produces: `logger.error` em toda degradação; `onDegraded` opcional nos métodos `listarTodos`/`listarTodas*`/`consultarTodos`/`todosPorTabela`.

- [ ] **Step 1: Escrever o teste**

Crie `tests/core/pagination-logging.test.ts`:

```ts
import { describe, expect, it, vi } from 'vitest';
import type { HttpClient } from '../../src/core/http.js';
import { ProdutosResource } from '../../src/resources/produtos.js';

function httpComRespostaDegradada() {
  return {
    restGet: vi.fn().mockResolvedValue({ inesperado: 'corpo sem a chave declarada' }),
    getLogger: vi.fn().mockReturnValue({
      debug: vi.fn(), info: vi.fn(), warn: vi.fn(), error: vi.fn(),
    }),
  } as unknown as HttpClient;
}

describe('sinal de degradacao', () => {
  it('marca degraded no resultado e emite logger.error', async () => {
    const http = httpComRespostaDegradada();
    const resource = new ProdutosResource(http);

    const resultado = await resource.listar();

    expect(resultado.degraded).toBe(true);
    expect(resultado.data).toEqual([]);
    expect(http.getLogger().error).toHaveBeenCalledTimes(1);
    expect(vi.mocked(http.getLogger().error).mock.calls[0]?.[0]).toContain('produtos');
  });

  it('resposta integra nao loga nada', async () => {
    const http = {
      restGet: vi.fn().mockResolvedValue({
        produtos: [{ codigoProduto: 1 }],
        pagination: { page: '0', offset: '0', total: '1', hasMore: 'false' },
      }),
      getLogger: vi.fn().mockReturnValue({
        debug: vi.fn(), info: vi.fn(), warn: vi.fn(), error: vi.fn(),
      }),
    } as unknown as HttpClient;
    const resource = new ProdutosResource(http);

    const resultado = await resource.listar();

    expect(resultado.degraded).toBe(false);
    expect(http.getLogger().error).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Rodar para ver falhar**

```bash
npx vitest run tests/core/pagination-logging.test.ts
```

Esperado: FAIL — nenhum log é emitido hoje.

- [ ] **Step 3: Confirmar que o log já sai**

O log de degradação foi implementado na Task 3 (dentro de `normalizePagination`) e ativado na Task 4. Os dois testes do Step 1 devem passar **sem nenhuma mudança de produção**.

```bash
npx vitest run tests/core/pagination-logging.test.ts
```

Se passarem, ótimo: eles ficam como teste de regressão e esta task se resume ao `onDegraded`. Se falharem, o log não está saindo por todos os caminhos — conserte antes de seguir e diga o que faltava na mensagem de commit.

- [ ] **Step 4: Expor `onDegraded` nas varreduras**

Nos 14 métodos que devolvem `AsyncGenerator`, acrescente o parâmetro opcional. Exemplo em `produtos.ts:181`:

```ts
  listarTodos(
    params?: Omit<ListarProdutosParams, 'page'> & { onDegraded?: (info: DegradedInfo) => void },
  ): AsyncGenerator<Produto> {
    const { onDegraded, ...filtros } = params ?? {};
    return createPaginator((page) => this.listar({ ...filtros, page }), 0, {
      onDegraded,
      logger: this.http.getLogger(),
    });
  }
```

Repita para os outros 13. `precos.todosPorTabela` mantém `startPage` 1 — o endpoint é 1-based (medido; `pagina=0` devolve HTTP 400).

- [ ] **Step 5: Rodar tudo**

```bash
npm run typecheck && npm run typecheck:tests && npm test
```

- [ ] **Step 6: Commit**

```bash
npm run lint:fix
git add src/ tests/
git commit -m "feat(pagination): callback onDegraded opt-in nas varreduras"
```

---

## Task 10: Teste de integração do resultado único

Critério 10 do spec. É o cenário que causou o incidente do consumidor e o único que atravessa SDK e API de verdade.

**Files:**
- Create: `tests/integration/pagination-resultado-unico.test.ts`

**Interfaces:**
- Consumes: SDK completo após as Tasks 4 e 5.
- Produces: nenhuma interface nova.

- [ ] **Step 1: Escrever o teste com timestamp derivado em runtime**

Crie `tests/integration/pagination-resultado-unico.test.ts`. **O timestamp nunca é hardcoded**: dado de sandbox muda, e um valor fixo faz o teste validar outra coisa ou falhar sem motivo.

```ts
import { beforeAll, describe, expect, it } from 'vitest';
import { SankhyaClient } from '../../src/index.js';

const client = new SankhyaClient({
  baseUrl: process.env.SANKHYA_BASE_URL as string,
  clientId: process.env.SANKHYA_CLIENT_ID as string,
  clientSecret: process.env.SANKHYA_CLIENT_SECRET as string,
  xToken: process.env.SANKHYA_X_TOKEN as string,
  logger: { level: 'silent' },
});

/** Datas do Sankhya vem como `dd/MM/yyyy HH:mm:ss` — ordenar exige converter. */
function chaveOrdenavel(data: string): string {
  const m = data.match(/(\d{2})\/(\d{2})\/(\d{4})\s+(\d{2}):(\d{2}):(\d{2})/);
  return m ? `${m[3]}${m[2]}${m[1]}${m[4]}${m[5]}${m[6]}` : '';
}

describe('resultado unico via modifiedSince', () => {
  let janelaDeUmRegistro: string | null = null;

  beforeAll(async () => {
    // Estreita a janela ate isolar exatamente 1 registro.
    let corte: string | undefined;
    for (let tentativa = 0; tentativa < 5; tentativa++) {
      const pagina = await client.produtos.listar(corte ? { modifiedSince: corte } : {});
      if (pagina.data.length === 1) {
        janelaDeUmRegistro = corte ?? null;
        return;
      }
      if (pagina.data.length === 0) return;

      const datas = pagina.data
        .map((p) => (p as unknown as Record<string, string>)['dataAlteracao:'])
        .filter(Boolean)
        .sort((a, b) => chaveOrdenavel(a).localeCompare(chaveOrdenavel(b)));
      const maior = datas[datas.length - 1];
      if (!maior || maior === corte) return;
      corte = maior;
    }
  }, 60_000);

  it('devolve o registro unico em vez de lista vazia', async () => {
    if (janelaDeUmRegistro === null) {
      // Nunca aprovar por omissao: diga em voz alta que nao foi verificado.
      console.warn('PULADO: sandbox nao tem janela com exatamente 1 alteracao');
      return;
    }

    const resultado = await client.produtos.listar({ modifiedSince: janelaDeUmRegistro });

    expect(resultado.data).toHaveLength(1);
    expect(resultado.totalRecords).toBe(1);
    expect(resultado.degraded).toBe(false);
  });
});
```

- [ ] **Step 2: Rodar contra o sandbox**

```bash
npm run test:integration -- tests/integration/pagination-resultado-unico.test.ts
```

Esperado: PASS. Se aparecer `PULADO`, o sandbox não tem janela adequada no momento — não é falha do código, mas registre e tente de novo depois; o critério não conta como verificado enquanto o teste pular.

- [ ] **Step 3: Commit**

```bash
npm run lint:fix
git add tests/integration/
git commit -m "test(integration): resultado único de modifiedSince chega ao consumidor"
```

---

## Task 11: Documentação e release

**Files:**
- Modify: `CHANGELOG.md`, `package.json`, `README.md`
- Modify: `src/resources/estoque.ts` (JSDoc), `src/resources/financeiros.ts` (JSDoc)

**Interfaces:**
- Consumes: tudo.
- Produces: release 1.5.0.

- [ ] **Step 1: Documentar a semântica de `total` por endpoint**

No JSDoc de `estoque.listar`, acrescente:

```ts
   * @remarks
   * `totalRecords` deste endpoint NAO e censo: conta produtos, enquanto o
   * array conta linhas por local de estoque. Medido: a pagina 0 devolve
   * 422 linhas com `total` 50. Nao use para verificar completude.
```

No JSDoc de `financeiros.listarReceitas` e `listarDespesas`:

```ts
   * @remarks
   * `totalRecords` deste endpoint E censo real (medido: 519004 receitas).
   * E a excecao — nos demais endpoints o campo conta apenas a pagina.
```

- [ ] **Step 2: Escrever o CHANGELOG**

Em `CHANGELOG.md`, no topo:

```markdown
## [1.5.0] - 2026-08-29

### Corrigido

- **Resultado único era descartado.** A API devolve objeto em vez de array quando exatamente 1 registro corresponde ao filtro; o SDK procurava um array e devolvia lista vazia, informando `totalRecords: 1`. Atingia qualquer listagem filtrada até um único resultado — em sync incremental, o caso comum.
- **Varreduras dos endpoints financeiros paravam na primeira página.** `/financeiros/receitas` e `/despesas` usam `hasMore` booleano; a comparação era com a string `'true'`. `listarTodasReceitas()` entregava 50 de 519.004 registros.
- **Varreduras de preços paravam na primeira página.** Os endpoints `/precos/*` não têm bloco `pagination` — trazem `temMaisRegistros` na raiz do corpo, que era ignorado.
- **`clientes.listar()` e `clientes.listarTodos()` pulavam a primeira página.** O default era `page: 1` num endpoint 0-based.
- **Iteração dirigida pelo echo do servidor.** `createPaginator` derivava a próxima página do campo `page` da resposta; agora usa contador local.
- **`total: "0"` virava `undefined`** em `normalizeRestPagination` e em `deserializeRows`.

### Adicionado

- `PaginatedResult.degraded` — `true` quando a resposta não trouxe o formato declarado pelo endpoint. Sempre presente no retorno do SDK. Lista vazia legítima vem com `false`.
- `onDegraded` opcional nos métodos de varredura, para instrumentação.
- `SankhyaConfig.onDegradedResponse` — hoje só `'flag'`. O default passa a `'throw'` na 2.0.0.
- Tipos `FinanceiroPagination` e `PrecosPagination`, documentando os contratos divergentes.

### Corrigido na documentação

- `modifiedSince` e `dataHoraAlteracao` documentavam formato ISO. A API exige `dd/MM/yyyy [HH:mm:ss]`; ISO responde `400 ORA-01861`.
- `totalRecords` documentado por endpoint: é censo nos financeiros e contagem de página nos demais.

### ⚠️ Impacto operacional

As varreduras antes truncadas passam a percorrer o conjunto inteiro. `listarTodasReceitas()` sai de 50 itens para 519.004 — **10.381 páginas**. Um job que hoje termina em segundos passará a fazer dez mil requisições. É correção de perda de dados, não regressão, mas planeje capacidade antes de subir. Varreduras acima de 100 páginas emitem `logger.warn`.

Se seu código espelha dados via sync incremental, **faça um resync completo** após o upgrade: o bug do resultado único deixou registros desatualizados sem rastro em log.
```

- [ ] **Step 3: Subir a versão**

Em `package.json`, `"version": "1.5.0"`.

- [ ] **Step 4: Verificar tudo**

```bash
npm run typecheck && npm run lint && npm test && npm run test:smoke
```

`test:smoke` reconstrói e carrega os bundles CJS e ESM — pega export quebrado que o typecheck não vê.

- [ ] **Step 5: Rodar os testes de integração**

```bash
npm run test:integration
```

Precisa de `.env`. Se a suíte tiver caso dependente das varreduras corrigidas, ele agora percorre muito mais páginas — confirme se algum precisa de `page` fixa para não virar teste lento.

- [ ] **Step 6: Commit**

```bash
npm run lint:fix
git add .
git commit -m "chore(release): 1.5.0 — paginação por contrato e sinal de degradação"
```

---

## Fora do escopo desta release

Vai para o plano da 2.0.0, com sua própria migração:

- `DegradedResponseError` e `onDegradedResponse: 'throw'` como default
- `extractRestRecordOrThrow` distinguindo degradação de not-found
- `deserializeRows` lançando nas saídas degradadas
- 404 de janela vazia mapeado para vazio legítimo (guarda tripla do spec D6)
- Normalizar a base de `PaginatedResult.page` entre contratos

E permanece fora de qualquer release enquanto não houver medição:

- Mapeamento de 404 em `pedidos.consultar` — o sandbox recusa a chamada (`LOGTABOPER` desabilitado)
