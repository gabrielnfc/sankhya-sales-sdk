---
phase: 05-test-coverage-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/resources/clientes.test.ts
  - tests/resources/vendedores.test.ts
  - tests/resources/produtos.test.ts
  - tests/resources/precos.test.ts
  - tests/resources/estoque.test.ts
  - tests/resources/fiscal.test.ts
  - tests/resources/client.test.ts
autonomous: true
requirements: [TEST-01, TEST-06]

must_haves:
  truths:
    - "Unit tests exist for ClientesResource covering listar, criar, atualizar, incluirContato, atualizarContato, listarTodos"
    - "Unit tests exist for VendedoresResource covering listar, buscar, listarTodos"
    - "Unit tests exist for ProdutosResource covering all 9+ methods"
    - "Unit tests exist for PrecosResource covering all 5 methods"
    - "Unit tests exist for EstoqueResource covering all 5 methods"
    - "Unit tests exist for FiscalResource covering calcularImpostos and importarNfse"
    - "Unit tests exist for SankhyaClient covering config validation, lazy-load getters, authenticate, invalidateToken"
    - "TipoPessoa F/J edge case is explicitly tested in clientes tests"
  artifacts:
    - path: "tests/resources/clientes.test.ts"
      provides: "ClientesResource unit tests with TipoPessoa edge case"
      min_lines: 80
    - path: "tests/resources/vendedores.test.ts"
      provides: "VendedoresResource unit tests"
      min_lines: 40
    - path: "tests/resources/produtos.test.ts"
      provides: "ProdutosResource unit tests"
      min_lines: 80
    - path: "tests/resources/precos.test.ts"
      provides: "PrecosResource unit tests"
      min_lines: 50
    - path: "tests/resources/estoque.test.ts"
      provides: "EstoqueResource unit tests"
      min_lines: 50
    - path: "tests/resources/fiscal.test.ts"
      provides: "FiscalResource unit tests"
      min_lines: 25
    - path: "tests/resources/client.test.ts"
      provides: "SankhyaClient unit tests"
      min_lines: 60
  key_links:
    - from: "tests/resources/clientes.test.ts"
      to: "src/resources/clientes.ts"
      via: "import ClientesResource"
      pattern: "import.*ClientesResource.*from"
    - from: "tests/resources/client.test.ts"
      to: "src/client.ts"
      via: "import SankhyaClient"
      pattern: "import.*SankhyaClient.*from"
---

<objective>
Write unit tests for the 6 simpler resources (clientes, vendedores, produtos, precos, estoque, fiscal) plus the SankhyaClient facade, including explicit TipoPessoa F/J edge case coverage.

Purpose: Cover ~320 lines of resource code and ~140 lines of client code with unit tests, addressing the largest chunk of the coverage gap. These resources are all REST-only (except fiscal which is simple POST), so they share the same mock pattern and can be tested efficiently together.

Output: 7 new test files in tests/resources/ covering all public methods of each resource and the client facade.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-test-coverage-hardening/05-RESEARCH.md
</context>

<interfaces>
<!-- Mock pattern from existing pedidos-idempotency.test.ts -->
```typescript
function createMockHttp() {
  return {
    restGet: vi.fn().mockResolvedValue({ /* resource-specific response */ }),
    restPost: vi.fn().mockResolvedValue({ /* resource-specific response */ }),
    restPut: vi.fn().mockResolvedValue({ /* resource-specific response */ }),
    gatewayCall: vi.fn().mockResolvedValue({}),
  } as unknown as HttpClient;
}
```

<!-- All resources follow this constructor pattern -->
```typescript
export class ClientesResource {
  constructor(private readonly http: HttpClient) {}
}
```

<!-- SankhyaClient config -->
```typescript
interface SankhyaConfig {
  baseUrl: string;
  clientId: string;
  clientSecret: string;
  xToken: string;
  timeout?: number;
  logger?: LoggerOptions;
  tokenCacheProvider?: TokenCacheProvider;
}
```

<!-- extractRestData + normalizeRestPagination return pattern -->
```typescript
// restGet returns raw response, resources call extractRestData + normalizeRestPagination
// Mock restGet must return { [resourceKey]: [...items], pagination: { page, total, hasMore, offset } }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for clientes, vendedores, produtos, precos, estoque, fiscal</name>
  <files>
    tests/resources/clientes.test.ts
    tests/resources/vendedores.test.ts
    tests/resources/produtos.test.ts
    tests/resources/precos.test.ts
    tests/resources/estoque.test.ts
    tests/resources/fiscal.test.ts
  </files>
  <read_first>
    src/resources/clientes.ts
    src/resources/vendedores.ts
    src/resources/produtos.ts
    src/resources/precos.ts
    src/resources/estoque.ts
    src/resources/fiscal.ts
    tests/resources/pedidos-idempotency.test.ts
    src/core/pagination.ts
  </read_first>
  <action>
Create unit test files for each resource using the createMockHttp() pattern from pedidos-idempotency.test.ts.

For each resource file, test every public method:

**clientes.test.ts** (6 methods):
- listar() calls restGet with '/parceiros/clientes' and default page '1'
- listar({ page: 2, dataHoraAlteracao: '...' }) passes query params correctly
- criar() calls restPost with '/parceiros/clientes' and input
- atualizar() calls restPut with '/parceiros/clientes/{id}' and input
- incluirContato() calls restPost with '/parceiros/clientes/{id}/contatos'
- atualizarContato() calls restPut with '/parceiros/clientes/{id}/contatos/{cid}'
- listarTodos() returns AsyncGenerator (verify first yield)
- **TEST-06 edge case**: listar() with TipoPessoa 'F' and 'J' values in response data (not 'PF'/'PJ')

**vendedores.test.ts** (3 methods):
- listar() with default and custom page
- buscar() calls restGet with '/vendedores/{id}' 
- listarTodos() returns AsyncGenerator

**produtos.test.ts** (9+ methods):
- listar(), buscar(), componentes(), alternativos(), volumes(), listarVolumes(), buscarVolume(), listarGrupos(), buscarGrupo(), listarTodos()
- Each verifies correct path and query params

**precos.test.ts** (5 methods):
- porTabela(), porProduto(), porProdutoETabela(), todosPorTabela(), contextualizado()
- Each verifies correct path and query params

**estoque.test.ts** (5 methods):
- porProduto(), listar(), listarLocais(), buscarLocal(), listarTodos()
- Each verifies correct path and query params

**fiscal.test.ts** (2 methods):
- calcularImpostos() calls restPost with correct path
- importarNfse() calls restPost with correct path

Mock pattern: restGet returns `{ [key]: [items], pagination: { page: '0', total: '1', hasMore: 'false', offset: '0' } }`. The resource key varies per resource (clientes, vendedores, produtos, etc.).

For listarTodos() tests: mock restGet to return hasMore 'false' on first call, then iterate with for-await-of and verify one item is yielded.

Use Biome-compliant style: single quotes, 100 char line width, 2-space indent, trailing commas.
  </action>
  <verify>
    <automated>npx vitest run tests/resources/clientes.test.ts tests/resources/vendedores.test.ts tests/resources/produtos.test.ts tests/resources/precos.test.ts tests/resources/estoque.test.ts tests/resources/fiscal.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -l "ClientesResource" tests/resources/clientes.test.ts returns match
    - grep -l "TipoPessoa\|tipo.*F.*J\|tipo: 'F'" tests/resources/clientes.test.ts returns match (TEST-06)
    - grep -l "VendedoresResource" tests/resources/vendedores.test.ts returns match
    - grep -l "ProdutosResource" tests/resources/produtos.test.ts returns match
    - grep -l "PrecosResource" tests/resources/precos.test.ts returns match
    - grep -l "EstoqueResource" tests/resources/estoque.test.ts returns match
    - grep -l "FiscalResource" tests/resources/fiscal.test.ts returns match
    - npx vitest run tests/resources/clientes.test.ts exits 0
    - npx vitest run tests/resources/vendedores.test.ts exits 0
    - npx vitest run tests/resources/produtos.test.ts exits 0
    - npx vitest run tests/resources/precos.test.ts exits 0
    - npx vitest run tests/resources/estoque.test.ts exits 0
    - npx vitest run tests/resources/fiscal.test.ts exits 0
  </acceptance_criteria>
  <done>All 6 resource test files exist and pass. Every public method of each resource is tested with at least one happy-path call verifying correct endpoint and params. TipoPessoa F/J edge case is explicitly covered in clientes tests.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for SankhyaClient facade</name>
  <files>
    tests/resources/client.test.ts
  </files>
  <read_first>
    src/client.ts
    src/types/config.ts
    src/resources/index.ts
  </read_first>
  <action>
Create tests/resources/client.test.ts testing the SankhyaClient facade:

**Config validation tests:**
- Creates instance with valid config (baseUrl, clientId, clientSecret, xToken all provided)
- Throws on missing/empty baseUrl with message containing 'baseUrl'
- Throws on missing/empty clientId with message containing 'clientId'
- Throws on missing/empty clientSecret with message containing 'clientSecret'
- Throws on missing/empty xToken with message containing 'xToken'

**Lazy-load getter tests:**
- client.clientes returns instanceof ClientesResource
- client.clientes returns same instance on second access (singleton)
- Same pattern for all 10 resources: vendedores, produtos, precos, estoque, pedidos, financeiros, cadastros, fiscal, gateway

**Public methods:**
- authenticate() calls auth.getToken() (need to verify via indirect means -- e.g., the client does not throw when auth succeeds)
- invalidateToken() calls auth.invalidateToken()

**Internal getters:**
- getHttpClient() returns HttpClient instance
- getAuthManager() returns AuthManager instance
- getLogger() returns Logger instance
- getConfig() returns the config object

Use validConfig constant: `{ baseUrl: 'https://api.test.com', clientId: 'test-id', clientSecret: 'test-secret', xToken: 'test-token' }`

Note: SankhyaClient constructor creates real AuthManager and HttpClient internally. Tests should verify behavior through the public API (getters return correct types, resources are lazy-loaded) without needing to mock internal construction. For authenticate/invalidateToken, these will attempt real HTTP calls -- since there is no server, they will throw. Test that the methods exist and are callable, or mock at the fetch level using vi.stubGlobal if needed.
  </action>
  <verify>
    <automated>npx vitest run tests/resources/client.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -l "SankhyaClient" tests/resources/client.test.ts returns match
    - grep -c "it\b\|test\b" tests/resources/client.test.ts shows >= 10 test cases
    - grep -l "validateConfig\|baseUrl.*obrigatório\|throws" tests/resources/client.test.ts returns match
    - grep -l "ClientesResource\|VendedoresResource" tests/resources/client.test.ts returns match
    - npx vitest run tests/resources/client.test.ts exits 0
  </acceptance_criteria>
  <done>client.test.ts exists with >= 10 tests covering config validation (4 error cases), all 10 resource lazy-load getters (singleton verified), and internal getter methods. All tests pass.</done>
</task>

</tasks>

<verification>
All 7 test files pass: `npx vitest run tests/resources/ --reporter=verbose`
TipoPessoa F/J edge case present in clientes tests
</verification>

<success_criteria>
- 7 new test files created in tests/resources/
- All tests pass green
- Every public method of clientes, vendedores, produtos, precos, estoque, fiscal, and SankhyaClient has at least one unit test
- TipoPessoa F/J explicitly tested (TEST-06 partial)
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-coverage-hardening/05-01-SUMMARY.md`
</output>
