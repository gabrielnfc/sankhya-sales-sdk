---
phase: 06-documentation
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - README.md
  - docs/guia/tratamento-erros.md
  - examples/01-quick-start.ts
  - examples/02-listar-produtos.ts
  - examples/03-criar-pedido.ts
  - examples/04-error-handling.ts
  - examples/05-gateway-generico.ts
  - examples/README.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-04
  - DOCS-05

must_haves:
  truths:
    - "README quick-start is completable in under 5 minutes of reading"
    - "Error handling guide shows try/catch examples with all 5 error classes and type guards"
    - "Every file in examples/ runs against sandbox with only env vars set"
  artifacts:
    - path: "README.md"
      provides: "Polished quick-start with install + auth + first call"
      contains: "Quick Start"
    - path: "docs/guia/tratamento-erros.md"
      provides: "Error handling guide with type guard examples"
      contains: "isApiError"
    - path: "examples/01-quick-start.ts"
      provides: "Runnable quick-start example"
      contains: "SankhyaClient"
    - path: "examples/04-error-handling.ts"
      provides: "Error handling example with all 5 error types"
      contains: "isGatewayError"
  key_links:
    - from: "README.md"
      to: "examples/"
      via: "links to example files"
      pattern: "examples/"
    - from: "examples/01-quick-start.ts"
      to: "sankhya-sales-sdk"
      via: "import statement"
      pattern: "from 'sankhya-sales-sdk'"
    - from: "docs/guia/tratamento-erros.md"
      to: "src/core/errors.ts"
      via: "documents error classes"
      pattern: "isApiError\\|isGatewayError\\|isAuthError"
---

<objective>
Polish README quick-start, update error handling guide with type guards, and create runnable examples/ directory.

Purpose: Complete the developer experience documentation (DOCS-01, DOCS-04, DOCS-05). After this plan, a new developer can install the SDK, follow the README, understand error handling, and reference working examples for every major use case.
Output: Polished README, updated error guide, 5 runnable example files + examples/README.md.
</objective>

<execution_context>
@C:/Users/gabri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gabri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation/06-RESEARCH.md
@.planning/phases/06-documentation/06-01-SUMMARY.md
@.planning/phases/06-documentation/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish README quick-start for 5-minute completability</name>
  <files>README.md</files>
  <read_first>
    - README.md (current full content -- understand existing structure before modifying)
    - src/client.ts (constructor signature for accurate config docs)
    - src/types/config.ts (SankhyaConfig interface fields)
    - package.json (verify package name, version)
  </read_first>
  <action>
Review and polish the existing README.md to ensure the quick-start section is completable in under 5 minutes. The README already has good structure -- this is a polish pass, not a rewrite.

Specific changes to make:

1. **Prerequisites section** (add if missing, after badges): Add a brief prerequisites note:
   ```markdown
   ## Pre-requisitos

   - Node.js >= 20
   - Credenciais OAuth 2.0 do Sankhya (Client ID, Client Secret, X-Token)
   ```

2. **Quick Start - env vars**: Ensure the quick-start section explicitly shows creating a `.env` file or setting env vars BEFORE the code example. Add this block if not present:
   ```markdown
   ### Variaveis de ambiente

   ```bash
   export SANKHYA_BASE_URL=https://api.sankhya.com.br
   export SANKHYA_CLIENT_ID=seu-client-id
   export SANKHYA_CLIENT_SECRET=seu-client-secret
   export SANKHYA_X_TOKEN=seu-x-token
   ```

3. **Quick Start code**: Verify the existing code example uses `process.env.*` (it does based on current README). Keep it.

4. **Error handling section**: Add a brief error handling section (or verify it exists) linking to the full guide:
   ```markdown
   ## Tratamento de Erros

   O SDK exporta type guards para identificar cada tipo de erro:

   ```typescript
   import { isApiError, isGatewayError, isAuthError } from 'sankhya-sales-sdk';

   try {
     await sankhya.pedidos.criar({ ... });
   } catch (error) {
     if (isAuthError(error)) {
       // Credenciais invalidas ou token expirado
     } else if (isApiError(error)) {
       console.error(`HTTP ${error.statusCode}: ${error.message}`);
     } else if (isGatewayError(error)) {
       console.error(`Erro Sankhya: ${error.sankhyaCode}`);
     }
   }
   ```

   Veja o [guia completo de tratamento de erros](./docs/guia/tratamento-erros.md).
   ```

5. **Examples link**: Add a link to the examples directory:
   ```markdown
   ## Exemplos

   Exemplos completos e executaveis em [`examples/`](./examples/):

   | Exemplo | Descricao |
   |---------|-----------|
   | [01-quick-start.ts](./examples/01-quick-start.ts) | Configuracao e primeira chamada |
   | [02-listar-produtos.ts](./examples/02-listar-produtos.ts) | Paginacao com listarTodos |
   | [03-criar-pedido.ts](./examples/03-criar-pedido.ts) | Fluxo completo de pedido |
   | [04-error-handling.ts](./examples/04-error-handling.ts) | Tratamento de cada tipo de erro |
   | [05-gateway-generico.ts](./examples/05-gateway-generico.ts) | CRUD via Gateway generico |
   ```

6. **TypeDoc link**: Add API reference link:
   ```markdown
   ## Referencia da API

   Gere a documentacao completa localmente:

   ```bash
   npm run docs
   open docs/api/index.html
   ```
   ```

7. **Do NOT** remove or restructure existing content that is already good (module table, feature list, etc.). Only add missing sections and polish existing ones.

After editing, verify the README reads naturally top-to-bottom: Install -> Pre-requisitos -> Quick Start -> Exemplos -> Erros -> API Reference -> Modulos.
  </action>
  <verify>
    <automated>cd "C:/Users/gabri/OneDrive/Desktop/projetos_true/sankhya_kit" && grep -q "Pre-requisitos\|Prerequisit" README.md && grep -q "SANKHYA_BASE_URL" README.md && grep -q "isApiError\|isGatewayError" README.md && grep -q "examples/" README.md && grep -q "npm run docs" README.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a prerequisites/pre-requisitos section mentioning Node.js >= 20
    - README.md contains env var setup showing SANKHYA_BASE_URL, SANKHYA_CLIENT_ID, SANKHYA_CLIENT_SECRET, SANKHYA_X_TOKEN
    - README.md contains error handling section with `isApiError` or `isGatewayError` type guard example
    - README.md contains link to `examples/` directory
    - README.md contains `npm run docs` reference for API reference generation
    - README.md existing Quick Start code block with `SankhyaClient` is preserved
    - README.md existing module/feature table is preserved (not removed)
  </acceptance_criteria>
  <done>README has complete quick-start flow (prerequisites -> install -> env vars -> code -> run) completable in under 5 minutes, plus error handling section, examples link, and API reference link.</done>
</task>

<task type="auto">
  <name>Task 2: Update error handling guide with type guard examples</name>
  <files>docs/guia/tratamento-erros.md</files>
  <read_first>
    - docs/guia/tratamento-erros.md (current guide content)
    - src/core/errors.ts (all 5 error classes + 5 type guards + SankhyaErrorCode type)
  </read_first>
  <action>
Update the existing error handling guide to include type guard usage examples. The guide already covers all 5 error classes -- add type guard examples.

Add the following sections/content (integrate into existing structure, do not duplicate):

1. **Type Guards section** (add after existing error class descriptions):
   ```markdown
   ## Type Guards

   O SDK exporta type guards para cada classe de erro. Use em blocos `catch` para narrowing seguro:

   ```typescript
   import {
     isSankhyaError,
     isAuthError,
     isApiError,
     isGatewayError,
     isTimeoutError,
   } from 'sankhya-sales-sdk';
   ```
   ```

2. **Complete try/catch example** covering all 5 error types:
   ```typescript
   try {
     const pedido = await sankhya.pedidos.criar({
       codigoEmpresa: 1,
       codigoCliente: 123,
       codigoTipoOperacao: 1000,
     });
   } catch (error) {
     if (isAuthError(error)) {
       // Credenciais invalidas ou token expirado
       // error.code === 'AUTH_ERROR'
       console.error('Falha na autenticacao:', error.message);
     } else if (isTimeoutError(error)) {
       // Timeout na requisicao
       // error.code === 'TIMEOUT_ERROR'
       console.error('Timeout:', error.message);
     } else if (isGatewayError(error)) {
       // Erro de negocio Sankhya (HTTP 200, mas erro no body)
       // error.sankhyaCode e error.level disponiveis
       console.error(`Erro Sankhya [${error.sankhyaCode}]: ${error.message}`);
     } else if (isApiError(error)) {
       // Erro HTTP (4xx/5xx)
       // error.statusCode, error.endpoint, error.method disponiveis
       console.error(`HTTP ${error.statusCode} em ${error.method} ${error.endpoint}`);
     } else if (isSankhyaError(error)) {
       // Qualquer outro erro do SDK
       console.error(`Erro SDK [${error.code}]: ${error.message}`);
     } else {
       throw error; // Erro desconhecido, propagar
     }
   }
   ```

3. **SankhyaErrorCode switch example:**
   ```typescript
   import type { SankhyaErrorCode } from 'sankhya-sales-sdk';

   function handleError(code: SankhyaErrorCode) {
     switch (code) {
       case 'AUTH_ERROR':
         // renovar credenciais
         break;
       case 'API_ERROR':
         // verificar endpoint e status
         break;
       case 'GATEWAY_ERROR':
         // erro de regra de negocio Sankhya
         break;
       case 'TIMEOUT_ERROR':
         // retry ou aumentar timeout
         break;
       default:
         // exhaustive check
         const _exhaustive: never = code;
     }
   }
   ```

4. **Per-error class details** (add to existing sections if not already present):
   - AuthError: mention `error.statusCode` may be 401 or undefined
   - ApiError: mention `error.endpoint` (string), `error.method` (string), `error.statusCode` (number)
   - GatewayError: mention `error.sankhyaCode` (string), `error.level` (string)
   - TimeoutError: mention it extends SankhyaError, thrown by AbortController

Do NOT remove existing content. Add type guard sections alongside existing error class documentation.
  </action>
  <verify>
    <automated>cd "C:/Users/gabri/OneDrive/Desktop/projetos_true/sankhya_kit" && grep -q "isApiError" docs/guia/tratamento-erros.md && grep -q "isGatewayError" docs/guia/tratamento-erros.md && grep -q "isAuthError" docs/guia/tratamento-erros.md && grep -q "isTimeoutError" docs/guia/tratamento-erros.md && grep -q "isSankhyaError" docs/guia/tratamento-erros.md && grep -q "SankhyaErrorCode" docs/guia/tratamento-erros.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - docs/guia/tratamento-erros.md contains `isApiError` usage example
    - docs/guia/tratamento-erros.md contains `isGatewayError` usage example
    - docs/guia/tratamento-erros.md contains `isAuthError` usage example
    - docs/guia/tratamento-erros.md contains `isTimeoutError` usage example
    - docs/guia/tratamento-erros.md contains `isSankhyaError` usage example
    - docs/guia/tratamento-erros.md contains `SankhyaErrorCode` switch/case example
    - docs/guia/tratamento-erros.md contains complete try/catch block showing all 5 error types
    - Existing error class documentation is preserved (not deleted)
  </acceptance_criteria>
  <done>Error handling guide includes type guard examples for all 5 error classes, SankhyaErrorCode switch pattern, and complete try/catch example covering every error type.</done>
</task>

<task type="auto">
  <name>Task 3: Create runnable examples/ directory with 5 example scripts</name>
  <files>examples/01-quick-start.ts, examples/02-listar-produtos.ts, examples/03-criar-pedido.ts, examples/04-error-handling.ts, examples/05-gateway-generico.ts, examples/README.md</files>
  <read_first>
    - src/index.ts (all public exports available for examples)
    - src/client.ts (SankhyaClient constructor signature)
    - src/resources/produtos.ts (listar, listarTodos method signatures)
    - src/resources/pedidos.ts (criar, adicionarItem, confirmar method signatures)
    - src/resources/gateway.ts (loadRecords, saveRecord method signatures)
    - src/core/errors.ts (error classes and type guards for error handling example)
    - src/types/config.ts (SankhyaConfig fields)
    - src/types/pedidos.ts (PedidoVendaInput, ItemPedidoInput)
    - src/types/gateway.ts (LoadRecordsParams, SaveRecordParams)
    - package.json (devDependencies to check if tsx needs installing)
  </read_first>
  <action>
Install tsx as devDependency and create 5 runnable example files + README.

1. Install tsx:
   ```
   npm install -D tsx
   ```

2. Create `examples/README.md`:
   ```markdown
   # Exemplos — sankhya-sales-sdk

   Scripts executaveis demonstrando as principais funcionalidades do SDK.

   ## Pre-requisitos

   Configure as variaveis de ambiente:

   ```bash
   export SANKHYA_BASE_URL=https://api.sankhya.com.br
   export SANKHYA_CLIENT_ID=seu-client-id
   export SANKHYA_CLIENT_SECRET=seu-client-secret
   export SANKHYA_X_TOKEN=seu-x-token
   ```

   ## Executando

   ```bash
   npx tsx examples/01-quick-start.ts
   ```

   ## Exemplos

   | Arquivo | Descricao |
   |---------|-----------|
   | 01-quick-start.ts | Configuracao basica e primeira chamada API |
   | 02-listar-produtos.ts | Paginacao manual e automatica com listarTodos |
   | 03-criar-pedido.ts | Fluxo completo: criar pedido, adicionar item, confirmar |
   | 04-error-handling.ts | Tratamento de cada tipo de erro com type guards |
   | 05-gateway-generico.ts | CRUD generico via Gateway (loadRecords/saveRecord) |
   ```

3. Create `examples/01-quick-start.ts`:
   ```typescript
   /**
    * Exemplo: Quick Start
    * Demonstra configuracao basica e primeira chamada API.
    * Run: npx tsx examples/01-quick-start.ts
    */
   import { SankhyaClient } from 'sankhya-sales-sdk';

   const sankhya = new SankhyaClient({
     baseUrl: process.env.SANKHYA_BASE_URL!,
     clientId: process.env.SANKHYA_CLIENT_ID!,
     clientSecret: process.env.SANKHYA_CLIENT_SECRET!,
     xToken: process.env.SANKHYA_X_TOKEN!,
   });

   async function main() {
     // Listar primeiros clientes
     const result = await sankhya.clientes.listar({ page: 0 });

     console.log(`Total de clientes: ${result.pagination.total}`);
     for (const cliente of result.data) {
       console.log(`- ${cliente.nome} (${cliente.codigoCliente})`);
     }
   }

   main().catch(console.error);
   ```

4. Create `examples/02-listar-produtos.ts`:
   ```typescript
   /**
    * Exemplo: Listar Produtos
    * Demonstra paginacao manual e iteracao automatica com listarTodos.
    * Run: npx tsx examples/02-listar-produtos.ts
    */
   import { SankhyaClient } from 'sankhya-sales-sdk';

   const sankhya = new SankhyaClient({
     baseUrl: process.env.SANKHYA_BASE_URL!,
     clientId: process.env.SANKHYA_CLIENT_ID!,
     clientSecret: process.env.SANKHYA_CLIENT_SECRET!,
     xToken: process.env.SANKHYA_X_TOKEN!,
   });

   async function main() {
     // Paginacao manual
     console.log('=== Pagina 1 ===');
     const page1 = await sankhya.produtos.listar({ page: 0 });
     console.log(`${page1.data.length} produtos, total: ${page1.pagination.total}`);

     // Iteracao automatica com listarTodos (AsyncGenerator)
     console.log('\n=== Todos os produtos (primeiros 10) ===');
     let count = 0;
     for await (const produto of sankhya.produtos.listarTodos()) {
       console.log(`${produto.codigoProduto} - ${produto.nome}`);
       if (++count >= 10) break;
     }
   }

   main().catch(console.error);
   ```

5. Create `examples/03-criar-pedido.ts`:
   ```typescript
   /**
    * Exemplo: Criar Pedido
    * Demonstra o fluxo completo: criar pedido, adicionar item, confirmar.
    * Run: npx tsx examples/03-criar-pedido.ts
    */
   import { SankhyaClient } from 'sankhya-sales-sdk';

   const sankhya = new SankhyaClient({
     baseUrl: process.env.SANKHYA_BASE_URL!,
     clientId: process.env.SANKHYA_CLIENT_ID!,
     clientSecret: process.env.SANKHYA_CLIENT_SECRET!,
     xToken: process.env.SANKHYA_X_TOKEN!,
   });

   async function main() {
     // 1. Descobrir um produto disponivel
     const produtos = await sankhya.produtos.listar({ page: 0 });
     const produto = produtos.data[0];
     if (!produto) throw new Error('Nenhum produto encontrado');
     console.log(`Produto: ${produto.nome} (${produto.codigoProduto})`);

     // 2. Criar pedido
     const pedido = await sankhya.pedidos.criar({
       codigoEmpresa: 1,
       codigoCliente: 1, // Ajuste para um cliente valido
       codigoTipoOperacao: 1000, // Ajuste para uma TOP valida
     });
     console.log(`Pedido criado: ${pedido.numeroPedido}`);

     // 3. Adicionar item ao pedido
     await sankhya.pedidos.adicionarItem({
       numeroPedido: pedido.numeroPedido,
       codigoProduto: produto.codigoProduto,
       quantidade: 1,
     });
     console.log('Item adicionado');

     // 4. Confirmar pedido
     await sankhya.pedidos.confirmar({
       numeroPedido: pedido.numeroPedido,
     });
     console.log('Pedido confirmado!');
   }

   main().catch(console.error);
   ```

6. Create `examples/04-error-handling.ts`:
   ```typescript
   /**
    * Exemplo: Tratamento de Erros
    * Demonstra como usar type guards para cada tipo de erro do SDK.
    * Run: npx tsx examples/04-error-handling.ts
    */
   import {
     SankhyaClient,
     isAuthError,
     isApiError,
     isGatewayError,
     isTimeoutError,
     isSankhyaError,
   } from 'sankhya-sales-sdk';

   const sankhya = new SankhyaClient({
     baseUrl: process.env.SANKHYA_BASE_URL!,
     clientId: process.env.SANKHYA_CLIENT_ID!,
     clientSecret: process.env.SANKHYA_CLIENT_SECRET!,
     xToken: process.env.SANKHYA_X_TOKEN!,
   });

   async function main() {
     try {
       // Tentar listar clientes
       const result = await sankhya.clientes.listar({ page: 0 });
       console.log(`Sucesso: ${result.data.length} clientes`);
     } catch (error) {
       if (isAuthError(error)) {
         console.error('Erro de autenticacao:', error.message);
         console.error('Verifique SANKHYA_CLIENT_ID e SANKHYA_CLIENT_SECRET');
       } else if (isTimeoutError(error)) {
         console.error('Timeout na requisicao:', error.message);
       } else if (isGatewayError(error)) {
         console.error(`Erro Sankhya [${error.sankhyaCode}]: ${error.message}`);
       } else if (isApiError(error)) {
         console.error(`HTTP ${error.statusCode} em ${error.method} ${error.endpoint}`);
       } else if (isSankhyaError(error)) {
         console.error(`Erro SDK [${error.code}]: ${error.message}`);
       } else {
         throw error;
       }
     }
   }

   main().catch(console.error);
   ```

7. Create `examples/05-gateway-generico.ts`:
   ```typescript
   /**
    * Exemplo: Gateway Generico
    * Demonstra CRUD via Gateway para qualquer entidade Sankhya.
    * Run: npx tsx examples/05-gateway-generico.ts
    */
   import { SankhyaClient } from 'sankhya-sales-sdk';

   const sankhya = new SankhyaClient({
     baseUrl: process.env.SANKHYA_BASE_URL!,
     clientId: process.env.SANKHYA_CLIENT_ID!,
     clientSecret: process.env.SANKHYA_CLIENT_SECRET!,
     xToken: process.env.SANKHYA_X_TOKEN!,
   });

   async function main() {
     // Carregar registros de uma entidade (ex: Produto)
     const result = await sankhya.gateway.loadRecords({
       entityName: 'Produto',
       fields: ['CODPROD', 'DESCRPROD', 'CODGRUPOPROD'],
       criteria: {
         expression: 'this.ATIVO = :ativo',
         parameters: [{ ativo: 'S' }],
       },
       page: 0,
       pageSize: 5,
     });

     console.log(`Total: ${result.totalRecords}`);
     for (const row of result.rows) {
       console.log(`${row.CODPROD} - ${row.DESCRPROD}`);
     }
   }

   main().catch(console.error);
   ```

All examples:
- Import from `'sankhya-sales-sdk'` (the package name, not relative paths)
- Use `process.env.*` for all configuration (no hardcoded values)
- Have a `main()` async function with `.catch(console.error)`
- Include a header comment with the run command
- Use single quotes (Biome convention)
- Follow the discovery pattern (listar first, then use results) instead of hardcoded IDs where possible
  </action>
  <verify>
    <automated>cd "C:/Users/gabri/OneDrive/Desktop/projetos_true/sankhya_kit" && test -f examples/01-quick-start.ts && test -f examples/02-listar-produtos.ts && test -f examples/03-criar-pedido.ts && test -f examples/04-error-handling.ts && test -f examples/05-gateway-generico.ts && test -f examples/README.md && grep -q "SankhyaClient" examples/01-quick-start.ts && grep -q "isApiError" examples/04-error-handling.ts && grep -q "gateway.loadRecords" examples/05-gateway-generico.ts && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - examples/01-quick-start.ts exists and imports from `'sankhya-sales-sdk'`
    - examples/02-listar-produtos.ts exists and uses `listarTodos()` AsyncGenerator
    - examples/03-criar-pedido.ts exists and calls `pedidos.criar()`, `adicionarItem()`, `confirmar()`
    - examples/04-error-handling.ts exists and uses all 5 type guards: isAuthError, isApiError, isGatewayError, isTimeoutError, isSankhyaError
    - examples/05-gateway-generico.ts exists and uses `gateway.loadRecords()`
    - examples/README.md exists with env var setup instructions and table of examples
    - All example files use `process.env.SANKHYA_BASE_URL!` (no hardcoded URLs)
    - All example files use single quotes (Biome convention)
    - package.json devDependencies contains `tsx`
    - README.md contains link to `examples/` directory
  </acceptance_criteria>
  <done>5 runnable example scripts created covering quick-start, pagination, order flow, error handling, and Gateway CRUD. README polished with prerequisites, env var setup, error handling section, examples link, and API reference link. Error handling guide updated with type guard examples.</done>
</task>

</tasks>

<verification>
- README.md has complete quick-start flow with env vars and error handling section
- docs/guia/tratamento-erros.md contains type guard examples for all 5 error classes
- All 5 example files exist and import from 'sankhya-sales-sdk'
- examples/README.md documents how to run examples
- `npx tsc --noEmit` still passes (no TypeScript errors introduced)
</verification>

<success_criteria>
A new developer can: (1) follow README quick-start in under 5 minutes, (2) find copy-pasteable error handling patterns in the guide, and (3) run any example file with `npx tsx examples/XX.ts` against a Sankhya sandbox.
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation/06-03-SUMMARY.md`
</output>
