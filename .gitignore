node_modules/
dist/
# Segredos: ignora todo .env e variantes (.env.local, .env.backup-*, .env.prod).
# `.env.example` e a unica excecao — nao tem valor real, so o formato.
.env
.env.*
!.env.example
*.tsbuildinfo
coverage/
.DS_Store
docs/api/
.claude/
CLAUDE.md

# Internal bug-tracking docs (not shipped, not public)
docs/bugs/
